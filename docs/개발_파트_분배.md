# 2조 개발 파트 분배 및 일정

| 항목 | 내용 |
|------|------|
| **작성일** | 2026-09-26 |
| **작성자** | 윤수연 (팀장) |
| **개발 기간** | 2026-09-28(월) 오후 \~ 10-01(목) |
| **최종 발표** | 2026-10-06(화) |

백엔드 기반(공통 코드·엔티티)과 프론트 뼈대는 `develop`에 올라가 있다. 9/28(월) 오후부터 각자 아래 기능을 개발하고, 10/1(목) 밤에 기능을 동결한 뒤 10/2(금)\~10/5(월)은 버그 수정과 발표 준비에 사용한다. 모든 팀원이 처음부터 끝까지 책임지는 기능을 맡으며, 작업 내용은 Git 커밋으로 남긴다.

---

## 0. 이미 준비된 것 (develop 브랜치)

각 기능은 아래 기반 위에 만든다. 실행 방법과 사용법은 각 폴더의 README에 있다.

| 영역 | 준비된 것 | 안내 |
|---|---|---|
| 백엔드 | Spring Boot 4.0.8(Java 17) 프로젝트, 패키지 구조, `application.properties`, 공통 응답(`ApiResponse`·`PageResponse`), 에러 코드 전체와 전역 예외 처리, 엔티티·enum 전체, 임시 보안 설정, 임시 `SecurityUtil`, 확인용 `/api/health` | [`backend/README.md`](../backend/README.md) |
| 관리자 화면 | Thymeleaf 레이아웃 뼈대, 확인용 `/admin/preview`, `/admin/login` 뼈대 | [`backend/README.md`](../backend/README.md) |
| 프론트엔드 | Vite + React, Tailwind·shadcn 설정, 폴더 구조, 라우터(코드 스플리팅), 레이아웃, ProtectedRoute, 토큰 헤더를 붙이는 axios, authStore 뼈대, MSW Mock 연결, `devLogin()` | [`frontend/README.md`](../frontend/README.md) |
| Mock 데이터 | API별 정상·오류 응답 JSON, 테스트 계정 | [`mock-data/README.md`](../mock-data/README.md) |

**킥오프 때 각자 확인할 것**

1. `git checkout develop` → `git pull`
2. MariaDB에 `kdsq_db` 생성 + `boot` 계정 권한 부여 (backend README 1단계)
3. 백엔드 실행 → http://localhost:8080/api/health 응답 확인
4. 프론트 `npm install` → `npm run dev` → 콘솔 `devLogin()` 후 `/surveys/p` 접속 확인

---

## 1. 담당표

기반 작업이 끝나 백엔드 분량을 다시 나눴다. 관리자 대시보드 통계는 윤수연이, 관리자 회원·검사 결과 관리는 이원구가 맡는다. 관리자 화면(Thymeleaf 템플릿)은 황지영이 두 사람의 컨트롤러에 연결한다.

| 이름 | 파트 | 담당 영역 | 주 참고 설계서 | 브랜치 예시 |
|---|---|---|---|---|
| 윤수연 | 백엔드 | 회원 API + 인증(JWT·Spring Security) + 관리자 대시보드 통계 | Entity 4.1·4.5·8.3·8.5·12장, REST 3장·4.1, UI 4.1·4.2 | `feature/be-member`, `feature/be-auth-jwt`, `feature/be-admin-dashboard` |
| 이주혁 | 백엔드 | 사용자 검사 API: 설문·문항 조회, 결과 제출·채점, 결과 상세, 내 검사 이력 | Entity 4.2\~4.4·8.0\~8.2·13.4, REST 4.2\~4.3 | `feature/be-survey-result` |
| 이원구 | 백엔드 | 관리자 회원 관리 + 관리자 검사 결과 관리 | Entity 8.4, UI 4.3\~4.7 | `feature/be-admin-member`, `feature/be-admin-result` |
| 강찬식 | 프론트엔드 | 인증 흐름(인터셉터·authStore·Header) + 로그인·회원가입·마이페이지 | React 3\~7장, UI 3.1·3.2·3.6 | `feature/fe-auth`, `feature/fe-mypage` |
| 서다영 | 프론트엔드 | 1차·2차 검사 + 검사 결과 화면 | React 3\~4장, UI 1.4·3.3\~3.5 | `feature/fe-screening-flow`, `feature/fe-result` |
| 황지영 | 프론트엔드 | 관리자 화면 Thymeleaf 템플릿 (레이아웃, 7개 화면, 차트, 에러 페이지) | UI 2.2\~2.5·4장·6\~8장 | `feature/fe-admin-thymeleaf` |

> 설계서 위치: [`필수제출문서/`](../필수제출문서)

---

## 2. 백엔드 상세

### 윤수연 — 회원 API + 인증 + 관리자 대시보드

**월\~화: 회원 API**

- [ ] 회원가입 `POST /api/members` (비밀번호 규칙·이메일 중복 409, REST 3.1.1)
- [ ] 내 정보 `GET /api/members/me`, 회원 탈퇴 `DELETE /api/members/me` (REST 4.1)
- [ ] AdminInitializer — 관리자 계정 등록 (Entity 12.1)

**화\~수: 인증**

- [ ] JwtTokenProvider, JwtAuthenticationFilter, 401 응답 3종(`ACCESS_TOKEN_EXPIRED`, `INVALID_REFRESH_TOKEN`, `UNAUTHORIZED`)
- [ ] RefreshToken 저장 + `login`·`reissue`·`logout` (Entity 4.5.3 흐름)
- [ ] SecurityConfig 정식본: 체인 2개(`/api/**` JWT, `/admin/**` formLogin), `@EnableMethodSecurity` (REST 3.3)
- [ ] `SecurityUtil.currentMemberId()`를 실제 토큰 기반으로 교체

**수: 관리자 대시보드**

- [ ] AdminDashboardController + AdminStatisticsService, 집계 쿼리 (Entity 8.3·8.5 코드), `MonthlyPointDto`
- [ ] Model 이름 `stats`로 `admin/dashboard` 화면에 전달 (5.3)

**목**: 통합 테스트, 백엔드 PR 검토·병합, 설계서와 달라진 부분 반영

> 참고: 강사님 백엔드 가이드 2.4절(Spring Security + JWT)에 설정 코드 예시가 있다.

### 이주혁 — 사용자 검사 API

- [ ] Repository: Survey·Question·SurveyResult·Solution (Entity 8.0 표의 사용자용 메서드)
- [ ] `data.sql` (Entity 13.4, `INSERT IGNORE`)
- [ ] `GET /api/surveys`(`examType` 필터), `GET /api/surveys/{surveyId}/questions`
- [ ] ScoringService + `POST /api/results`: 1차 종료·2차 포함 제출, 422·400·404 처리
- [ ] `GET /api/results/{resultId}`: fetch join, 본인·활성 결과가 아니면 404, `solutions` 포함
- [ ] `GET /api/members/me/results`: 최신순 페이징, fetch join + countQuery
- [ ] 테스트: Entity 10장 단위 테스트, REST 7.4의 TC-SURV·TC-RES 시나리오 (`mock-data/requests`, `mock-data/responses` 활용)

### 이원구 — 관리자 회원 관리 + 검사 결과 관리

설계서에는 조회 쿼리 코드가 있고, 컨트롤러·서비스·폼 DTO·검증·리다이렉트는 직접 작성한다.

**월\~화: 회원 관리 (ADM-03·04·04-1)**

- [ ] 컨트롤러 뼈대와 샘플 Model을 먼저 올려 황지영이 화면을 바로 연결할 수 있게 한다 (5.3의 Model 이름 사용)
- [ ] 목록: 이름·이메일 검색 + 페이징 (`searchMembers`)
- [ ] 상세: 회원 정보 + 검사 이력 (`findActiveByMemberId`)
- [ ] 수정 폼 GET/POST: `@Valid` + `BindingResult`, 이메일 중복 검사(`existsByEmailAndIdNot`)
- [ ] 탈퇴 처리 POST: `member.withdraw()`, 리다이렉트 + 플래시 메시지

**수: 검사 결과 관리 (ADM-05·06)**

- [ ] 목록: Specification 동적 검색(검사 종류·위험도·이름·기간) + 페이징, 검색 조건 유지
- [ ] 상세: `findActiveDetailById`
- [ ] 삭제 POST: `deactivate()` (소프트 삭제), 리다이렉트 + 플래시 메시지
- [ ] 관리자 서비스에 `@PreAuthorize("hasRole('ADMIN')")` (REST 3.3)

---

## 3. 프론트엔드 상세

프론트는 백엔드를 기다리지 않고 Mock 데이터(MSW)로 수요일까지 개발한 뒤, 목요일에 실제 API로 바꾼다. 로그인 화면이 완성되기 전에는 브라우저 콘솔의 `devLogin()`으로 테스트 로그인한다.

### 강찬식 — 인증 흐름 + 로그인·회원가입·마이페이지

프론트 공통 파일(`package.json`, `vite.config.js`, `App.jsx`, `axiosInstance`, `authStore`)의 주인이다.

- [ ] axios 응답 인터셉터: `ACCESS_TOKEN_EXPIRED` → 재발급 후 1회 재시도, 그 외 401 → 로그아웃 (React 5.3)
- [ ] authStore `login()`·`logout()`, Header 로그인 상태별 메뉴·로그아웃·모바일 메뉴
- [ ] LoginPage·LoginForm (401·403 문구)
- [ ] RegisterPage (입력 검증 4.2, `utils/validation.js`, 서버 `fields` 표시, 409)
- [ ] MyPage: ProfileCard, ExamHistoryTable(10건씩), ExamTrendChart(Recharts, 5.5), EmptyHistory, WithdrawDialog, memberStore
- [ ] 프론트 PR 검토·병합

### 서다영 — 검사 + 검사 결과

- [ ] surveyStore, resultStore(`pendingFirstAnswers` sessionStorage 유지), `utils/kdsq.js`
- [ ] SurveyPage(`type` P/C): SurveyHeader(안내 문구), ProgressBar, QuestionCard, AnswerOptions(원문 선택지), 이전·다음·제출 버튼
- [ ] 분기: 1차 0\~3점 제출, 4점 이상 보관 후 `/surveys/c`, 1차 답변 없이 `/surveys/c` 진입 시 되돌리기, 422 처리
- [ ] ResultPage: 판정 문구, 총점, 영역별 점수(KDSQ-C만), 검사일시, 관리 안내(`solutions`), 버튼
- [ ] 검사 화면 반응형(UI 7장)·접근성(UI 8장): 모바일에서 선택지 세로 배치, 버튼 높이 48px, 키보드 이동

### 황지영 — 관리자 화면 (Thymeleaf)

템플릿은 백엔드 프로젝트의 `src/main/resources/templates`에 둔다 (UI 2.5 파일 구조). 이미 있는 레이아웃·로그인 뼈대를 설계서에 맞춰 완성한다.

- [ ] `layout/admin-layout.html` + 프래그먼트(header·sidebar·breadcrumb·flash·pagination·footer), `admin.css`, `admin.js`
- [ ] ADM-01 로그인(`?error`·`?logout` 상태)
- [ ] ADM-03 회원 목록(검색어 하이라이트), ADM-04 상세, ADM-04-1 수정 폼(`th:object`·`th:errors`) — 이원구 컨트롤러와 연결
- [ ] ADM-05 검사 결과 목록(필터 유지·하이라이트), ADM-06 상세(판정 기준 표시) — 이원구 컨트롤러와 연결
- [ ] ADM-02 대시보드(지표 카드, Chart.js 도넛·월별 추이, 최근 위험 목록) — 윤수연 컨트롤러와 연결
- [ ] 에러 페이지 403·404·500, 탈퇴·삭제는 POST 폼 + 확인창
- [ ] 관리자 화면이 완성되면 확인용 `admin/preview.html`과 `WebConfig`의 `/admin/preview`를 삭제

> 컨트롤러가 올라오기 전에는 레이아웃·프래그먼트·CSS를 먼저 만들고, `/admin/preview`로 화면을 확인한다. Thymeleaf는 강사님 가이드의 Thymeleaf 문서 7장 예시를 참고한다.

---

## 4. 일정

모든 API는 수요일 밤까지 완성하고, 목요일은 프론트와 백엔드를 실제로 연결하는 날로 비워 둔다. **목요일 밤 기능 동결.**

| 날짜 | 백엔드 | 프론트엔드 | 그날 끝나야 하는 것 |
|---|---|---|---|
| 9/28(월) 오후 | 회원 API, 관리자 회원 관리 컨트롤러 뼈대 | 인증 흐름·로그인, 검사 컴포넌트, 관리자 레이아웃·프래그먼트 | 각자 로컬 실행 확인 후 첫 커밋 |
| 9/29(화) | 회원 API 완료·JWT 시작, 설문 API·data.sql, 관리자 회원 관리 | 로그인·회원가입, SurveyPage, 관리자 회원 관리 화면 | 회원가입 API, 설문·문항 API, 관리자 회원 목록 화면 |
| 9/30(수) | 로그인·재발급·로그아웃·보안 설정 정식본, 대시보드, 결과 API 3개, 검사 결과 관리 | 마이페이지, ResultPage, 관리자 검사 결과·대시보드 화면 | **모든 API 완성**, 모든 화면이 Mock으로 동작 |
| 10/1(목) | 전원 통합: 실제 API 연결, REST 7.4 테스트 케이스 확인, 관리자 화면 끝까지 확인 | (왼쪽과 같음) | **기능 동결** (밤) |
| 10/2(금) | 버그 수정 | 버그 수정, 반응형·접근성 점검 | README, 화면 캡처(UI 설계서 5장), 발표자 확정 |
| 10/3(토)\~10/4(일) | 발표 준비 | 발표 준비 | 발표 PPT, 시연 시나리오, 리허설 |
| 10/5(월) | 최종 점검 | 최종 점검 | develop → main 병합, 설계서 최종본, 개인 회고록 |
| 10/6(화) | 최종 발표 | 최종 발표 | |

---

## 5. 파트 간 약속

### 5.1 파일 주인

여러 사람이 동시에 수정하면 충돌하는 파일은 주인을 한 명으로 정한다. 다른 사람은 수정이 필요하면 주인에게 요청한다.

| 담당 | 파일 |
|---|---|
| 윤수연 | `build.gradle`, `application.properties`, `ErrorCode`, `ApiResponse`, `SecurityConfig`, `SecurityUtil` |
| 이주혁 | `data.sql` |
| 강찬식 | `package.json`, `vite.config.js`, `App.jsx`(라우터), `axiosInstance`, `authStore` |

### 5.2 임시 인증

JWT가 완성되기 전까지 로그인한 회원 id는 `SecurityUtil.currentMemberId()`로만 얻는다 (지금은 항상 1). 테스트 전에 회원가입 API로 회원을 1명 만들어 둔다. JWT가 완성되면 이 함수의 내부만 교체하므로, 이를 사용하는 다른 코드는 고칠 필요가 없다.

### 5.3 관리자 화면 요청·Model 이름

| 요청 | 템플릿 | Model 이름 | 서버 담당 |
|---|---|---|---|
| GET `/admin/login` | `admin/login` | (파라미터 `error`, `logout`) | 윤수연 (formLogin) |
| GET `/admin/dashboard` | `admin/dashboard` | `stats` (DashboardStatsDto) | 윤수연 |
| GET `/admin/members?keyword=&page=` | `admin/members/list` | `members` (Page), `keyword` | 이원구 |
| GET `/admin/members/{id}` | `admin/members/detail` | `member`, `results` | 이원구 |
| GET·POST `/admin/members/{id}/edit` | `admin/members/edit` | `form`, `memberId` | 이원구 |
| POST `/admin/members/{id}/withdraw` | → 상세로 리다이렉트 | 플래시 `successMessage` / `errorMessage` | 이원구 |
| GET `/admin/results?examType=&riskLevels=&name=&from=&to=&page=` | `admin/results/list` | `results` (Page), `cond` (검색 조건) | 이원구 |
| GET `/admin/results/{id}` | `admin/results/detail` | `result` | 이원구 |
| POST `/admin/results/{id}/delete` | → 목록으로 리다이렉트 | 플래시 `successMessage` / `errorMessage` | 이원구 |

### 5.4 Git 규칙 (강사님 브랜치 전략)

- 작업 전 `git checkout develop` → `git pull`로 최신 상태를 받고, `develop`에서 `feature/be-…`, `feature/fe-…` 브랜치를 만든다.
- 작업이 끝나면 PR로 `develop`에 합친다. **PR을 만들 때 base가 `develop`인지 꼭 확인한다.** `main`은 10/5에만 병합한다.
- PR 검토·병합: 백엔드 윤수연, 프론트엔드 강찬식
- 커밋 메시지: `타입: 요약` (`feat`, `fix`, `docs`, `refactor`, `test`, `chore`)
- 기능 하나·파일 묶음 하나 단위로 하루에 여러 번 커밋한다. 개별 평가가 커밋 기록을 기준으로 하므로 하루치 작업을 모아 한 번에 올리지 않는다.
- `application-local.properties`, `.env.development.local` 같은 개인 설정 파일은 올리지 않는다 (`.gitignore`에 등록됨).
- 매일 저녁 디스코드에 **오늘 한 일 / 내일 할 일 / 막힌 점**을 한 줄씩 남긴다.

---

## 6. 일정이 밀릴 때

수요일 저녁에 파트별 진척을 한 번 점검하고, 밀린 작업은 아래 기준으로 나눠 맡는다.

| 상황 | 확인 시점 | 대응 |
|---|---|---|
| 사용자 검사 API 지연 | 9/30(수) 저녁 | 제출·채점을 먼저 끝내고, 남은 조회 API(결과 상세·내 이력)는 백엔드 팀 안에서 나눠 맡는다 |
| JWT·보안 설정 지연 | 9/30(수) 저녁 | 임시 인증으로 다른 기능은 계속 개발한다. 대시보드를 백엔드 팀 안에서 넘기고, 목요일 오전에 프론트·백엔드 경험자가 함께 마무리한다 |
| 관리자 회원·결과 관리 지연 | 9/30(수) 저녁 | 검사 결과 관리 중 남은 부분을 백엔드 팀 안에서 나눠 맡는다 |
| 관리자 화면이 컨트롤러를 기다림 | 9/29(화) | 컨트롤러 뼈대와 샘플 Model을 먼저 올린다. 그 전에는 `/admin/preview`로 레이아웃을 확인한다 |
| 목요일 하루로 통합이 부족 | 9/30(수)부터 | 완성된 API부터 Mock을 끄고 바로 연결해 본다 (API 단위로) |
| 설계서와 다르게 구현해야 함 | 수시 | 바꾸기 전에 디스코드에 알리고 설계서를 함께 고친다 (발표 때 설계서와 코드가 같아야 함) |
