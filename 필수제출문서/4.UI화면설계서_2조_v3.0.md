# UI 화면 설계서 — 인지기능검사 서비스

## 문서 정보
| 항목 | 내용 |
|------|------|
| **프로젝트명** | 인지기능검사 서비스 |
| **작성자** | 강찬식 서다영 황지영 |
| **작성일** | 2026-09-23 |
| **버전** | v3.1 |
| **검토자** | 강찬식 서다영 황지영 |

---

## 1. 화면 목록 개요

### 1.1 사용자(User) 화면 목록

| 화면 ID | 페이지명 | URL | 인증 | 주요 기능 | 필요한 API |
| --- | --- | --- | --- | --- | --- |
| SCR-01 | 로그인   | `/login`          | X  | 이메일/비밀번호 입력, JWT 발급, 회원가입 이동 | `POST /api/auth/login` |
| SCR-02 | 회원가입  | `/register`       | X  | 회원정보(이름/이메일/비밀번호/성별/출생년도) 입력 및 등록 | `POST /api/auth/register` |
| SCR-03 | 검사 메인(1차) | `/surveys`        | O  | KDSQ-P 검사, firstScore 저장   | `GET /api/surveys/p` `POST /api/exams/p` |
| SCR-04 | 상세 검사(2차) | `/surveys/detail` | O  | KDSQ-C 검사, 영역별 점수 저장   | `GET /api/surveys/detail/c` `POST /api/exams/c` |
| SCR-05 | 검사 결과 | `/results`        | O  | 결과/위험도/솔루션(주의·위험 공통) | `GET /api/results` |
| SCR-06 | 마이페이지 | `/mypage`         | O  | 회원정보 조회, 검사 이력  | `GET /api/members/me` `GET /api/exams/history` |
| (공통) | 로그아웃 | — | O | 토큰 폐기 후 `/login` 이동 | `POST /api/auth/logout` |

### 1.2 관리자(Admin) 화면 목록

| 화면 ID | 화면명 | URL 경로 | 인증 | 주요 기능 |
| --- | --- | --- | --- | --- |
| ADM-01 | 관리자 로그인 | `/admin/login` | X | ID/비밀번호 입력 |
| ADM-02 | 대시보드 | `/admin/dashboard` | O(관리자) | 핵심 지표 모니터링 |
| ADM-03 | 회원 목록 | `/admin/members` | O(관리자) | 회원정보 조회, 검색 |
| ADM-03-1 | 회원 수정 | `/admin/members/{id}/edit` | O(관리자) | 회원정보 수정(이름/이메일/성별/출생년도) | 
| ADM-04 | 회원 상세 | `/admin/members/{id}` | O(관리자) | 검사 이력 조회 |
| ADM-05 | 검사 결과 목록 | `/admin/exams` | O(관리자) | 검사 결과 조회, 검색(이름/날짜/검사종류), 삭제 |
| ADM-06 | 검사 상세 결과 | `/admin/exams/{id}` | O(관리자) | 상세 결과 조회, 삭제 |

### 1.3 권한별 접근 정책

| 권한 | 접근 범위 | 비고 |
|---|---|---|
| 공개 | 로그인, 회원가입, 관리자 로그인 | 검사 및 마이페이지 접근 불가 |
| 사용자 | 공개 화면 + 검사 + 결과 + 마이페이지 | 회원정보는 조회 전용 |
| 관리자 | 관리자 로그인 후 관리자 전체 화면 | 회원정보·검사결과 관리 가능 |
| 개발자(유지보수) | 화면 설계 범위 외 | 운영 데이터 조회 권한 없음 |

### 1.4 핵심 검사 흐름

```text
회원가입
   ↓
로그인
   ↓
KDSQ-P (1차) 5문항
   ↓
firstScore 산출 (10점 만점)
   ├── 0~3점 → 정상 → 결과 화면 → 종료
   │
   └── 4~10점 → KDSQ-C (2차) 진행
                    ↓
             15문항 응답
                    ↓
      memoryScore + otherScore + adlScore 산출
             (각 10점 만점, 저장)
                    ↓
      totalScore = memory + other + adl
             (30점 만점, 프론트에서 계산)
                    ↓
             ┌──────┴──────┐
             ↓             ↓
           0~5점          6~30점
            주의           고위험군
             └──────┬──────┘
                    ↓
             결과 화면
        (관리·추가 검진 안내 포함)
                    ↓
                 마이페이지
```

> 판정 기준은 문서 전체에서 **KDSQ-P 0\~3 정상 / 4\~10 2차 대상**, **KDSQ-C 0\~5 주의 / 6\~30 고위험군**으로 통일한다.

---

# 2. 전체 레이아웃 구조

## 2.1 사용자 레이아웃

```text
┌─────────────────────────────────────────────────────────────┐
│  Header                                                     │
│  [KDSQ]         [검사하기]   [마이페이지]   [로그인/로그아웃]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                     Main Content Area                       │
│                     (각 페이지 내용)                         │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  Footer: ⓒ 2026 마음 기억                                   │
└─────────────────────────────────────────────────────────────┘
```

## 2.2 관리자 레이아웃

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ Header                                                                   │
│ [KDSQ 관리자]        [대시보드]  [회원관리]  [검사결과]  [로그인/로그아웃] │
├──────────────┬───────────────────────────────────────────────────────────┤
│ 사이드바      │                    Main Content Area                     │
│ 대시보드      │                    (각 관리자 화면)                       │
│ 회원관리      │                                                           │
│ 검사결과      │                                                           │
├──────────────┴───────────────────────────────────────────────────────────┤
│ Footer: ⓒ 2026 마음 기억                                                 │
└──────────────────────────────────────────────────────────────────────────┘
```

## 2.3 공통 컴포넌트

| 컴포넌트 | 영역 | 역할 |
|---|---|---|
| `Header` | User | 로고, 네비게이션, 로그인 상태 |
| `LoadingSpinner` | User | 데이터 로딩 표시 |
| `Alert` | User | 오류/안내 메시지 |
| `ProtectedRoute` | User | 로그인 필요 화면 접근 제어 |
| `AdminLayout` | Admin | 관리자 공통 레이아웃 |
| `Sidebar` | Admin | 대시보드/회원관리/검사결과 메뉴 |
| `Footer` | 공통 | 저작권/하단 정보 |

---

# 3. 사용자 화면별 상세 설계

## 3.1 SCR-01 — 로그인 화면

### 3.1.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 로그인 |
| **URL** | `/login` |
| **화면 타입** | 입력 |
| **권한 레벨** | 공개 |
| **주요 기능** | 등록된 이메일과 비밀번호로 로그인 |
| **연결 API** | `POST /api/auth/login` |

### 3.1.2 와이어프레임

```text
┌──────────────────────────────────────────────┐
│ Header                                       │
├──────────────────────────────────────────────┤
│                    KDSQ                      │
│              인지선별검사 플랫폼               │
│                                              │
│   이메일                                      │
│   ┌──────────────────────────────────────┐   │
│   │ example@email.com                    │   │
│   └──────────────────────────────────────┘   │
│                                              │
│   비밀번호                                    │
│   ┌──────────────────────────────────────┐   │
│   │ ⚠ 이메일 또는 비밀번호를 확인해주세요. │   │  ← error 시에만 노출
│   └──────────────────────────────────────┘   │
│                                              │
│   ┌──────────────────────────────────────┐   │
│   │                 로그인                │   │  ← loading 시 비활성 + 스피너
│   └──────────────────────────────────────┘   │
│                                              │
│               [ 회원가입하기 ]                │
│                                              │
└──────────────────────────────────────────────┘
```

### 3.1.3 UI 컴포넌트 매핑

| 화면 요소      | 컴포넌트        | 라이브러리 컴포넌트        | 역할                               |
| :--------- | :---------- | :---------------- | :------------------------------- |
| 로그인 화면     | `LoginPage` | -                 | 화면 컨테이너, 로그인 성공 시 `/surveys`로 이동 |
| 로그인 폼      | `LoginForm` | -                 | 이메일·비밀번호 입력 및 로그인 처리             |
| 이메일 입력창    | `Input`     | `Input` (shadcn)  | 이메일 입력 (`type="email"`)          |
| 비밀번호 입력창   | `Input`     | `Input` (shadcn)  | 비밀번호 입력 (`type="password"`)      |
| 로그인        | `Button`    | `Button` (shadcn) | 로그인 제출, 로딩 시 비활성화 및 스피너 표시       |
| 로그인 실패 메시지 | `Alert`     | `Alert` (shadcn)  | 로그인 실패 메시지 표시                    |
| 회원가입하기     | `Link`      | `Link` (shadcn)   | 회원가입 화면으로 이동                     |

### 3.1.4 화면 상태

| 상태  | 표시 내용  | 트리거     |
| ------ | --------- | ----------- |
| **초기 상태** | 이메일/비밀번호 입력 폼 및 로그인 버튼 표시   | 로그인 화면 진입           |
| **입력 중**  | 사용자가 입력한 이메일/비밀번호 표시          | 각 입력 필드에 값 입력       |
| **입력칸 공백**  | "이메일과 비밀번호를 입력해주세요." 메시지 표시   | 필수값 누락 (제출 차단)       |
| **입력 오류** | "올바른 이메일 형식이 아닙니다." 메시지 표시   | 이메일 형식 오류 (제출 차단)    |
| **로그인 진행 중** | 버튼 비활성화 + CircularProgress       | 로그인 버튼 클릭           |
| **로그인 성공**  | 로그인 처리 후 검사 화면(/surveys)으로 이동         | 로그인 API 성공          |
| **로그인 인증 실패-401**  | "이메일 또는 비밀번호를 확인해주세요." 메시지 표시   | 이메일 또는 비밀번호 불일치     |
| **탈퇴 회원-403**  | "이용할 수 없는 계정입니다." 메시지 표시   | 이메일 또는 비밀번호 불일치     |
| **네트워크 오류-5xx** | "일시적인 오류입니다. 잠시 후 다시 시도해주세요." 메시지 표시 | 서버 연결 실패            |
| **회원가입 이동** | 회원가입 화면(/register)으로 이동                | "회원가입하기" 클릭         |

> 보안상 존재하지 않는 이메일과 비밀번호 불일치를 구분해 노출하지 않는다.

### 3.1.5 사용자 인터랙션 흐름

```text
[authStore.isAuthenticated 확인]
   → [false] → [/login]
               ↓
         [이메일/비밀번호 입력]
           → [입력값 상태 업데이트]
           → [로그인 버튼 클릭] → loading = true
                 → [POST /api/auth/login]
                    → [200] → [토큰 저장(authStore)-로그인 처리 + /surveys 이동]
                    → [401] → [error: "이메일 또는 비밀번호를 확인해주세요."]
                    → [5xx] → [error: "일시적인 오류입니다. 잠시 후 다시 시도해주세요."]
   → [true] → [/surveys]

[회원가입하기 클릭]
    → [회원가입 화면으로 이동]
```

---

## 3.2 SCR-02 — 회원가입 화면

### 3.2.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 회원가입 |
| **URL** | `/register` |
| **화면 타입** | 입력 |
| **권한 레벨** | 공개 |
| **주요 기능** | 이름/이메일/비밀번호/성별/출생년도 입력 및 등록 |
| **연결 API** | `POST /api/auth/register` |

### 3.2.2 와이어프레임

```text
┌──────────────────────────────────────────────┐
│ Header                                       │
├──────────────────────────────────────────────┤
│                  회원가입                     │
│                                              │
│   이름 *                                      │
│   ┌──────────────────────────────────────┐   │
│   │ 홍길동                                │   │
│   └──────────────────────────────────────┘   │
│                                              │
│   이메일 *                                    │
│   ┌──────────────────────────────────────┐   │
│   │ example@email.com                    │   │
│   └──────────────────────────────────────┘   │
│   └ 이미 가입된 이메일입니다.                   │  ← 중복 시 helperText
│                                              │
│   비밀번호 *                                  │
│   ┌──────────────────────────────────────┐   │
│   │ ●●●●●●●●                             │   │
│   └──────────────────────────────────────┘   │
│                                              │
│   비밀번호 확인 *                              │
│   ┌──────────────────────────────────────┐   │
│   │ ●●●●●●●●                             │   │
│   └──────────────────────────────────────┘   │
│   └ 비밀번호가 일치하지 않습니다.               │
│                                              │
│   성별 *                                      │
│    ○ 남성        ○ 여성                       │
│                                              │
│   출생년도 *                                  │
│   ┌────────────┐                             │
│   │ 1960       │ 년                          │
│   └────────────┘                             │
│                                              │
│   ┌──────────────────────────────────────┐   │
│   │               회원가입                │   │
│   └──────────────────────────────────────┘   │
│                                              │
│              ← 로그인으로                     │
└──────────────────────────────────────────────┘
```

### 3.2.3 UI 컴포넌트 매핑

| 화면 요소       | 컴포넌트         | 라이브러리 컴포넌트    | 필수 | 역할                  |
| ----------------| ---------------- | --------------------- | ---- | --------------------- |
| 이름 입력창      | `Name`            | `Input` (shadcn)      |  O  | 사용자 이름 입력        |
| 이메일 입력창    | `Email`           | `Input` (shadcn)      |  O  | 이메일 입력             |
| 비밀번호 입력창  | `Password`        | `Input` (shadcn)      |  O  | 비밀번호 입력           |
| 비밀번호 확인    | `PasswordConfirm` | `Input` (shadcn)      |  O  | 비밀번호 일치 여부 검증 |
| 성별 선택        | `Gender`          | `RadioGroup` (shadcn) |  O  | 남성/여성 중 하나 선택  |
| 출생년도 입력창  | `BirthYear`       | `Input` (shadcn)       |  O  | 출생년도 숫자 입력     |
| 로그인           | `Button`          | `Button` (shadcn)      |  -  | 로그인 하기            |
| 회원가입하기     | `Link`             | `Link` (shadcn)        |  -  | 회원가입 창으로 이동   |

### 3.2.4 화면 상태

| 상태            | 표시 내용                                 | 트리거                 |
| ------------- | ------------------------------------- | ------------------- |
| **초기 상태**     | 이름/이메일/비밀번호/비밀번호 확인/성별/출생년도 입력 폼 표시   | 회원가입 화면 진입          |
| **입력 중**      | 사용자가 입력한 값 표시                         | 각 입력 필드에 값 입력       |
| **입력 오류**     | 해당 TextField helperText + error 상태 표시          | 필수값 누락 또는 입력 형식 오류 (제출 차단)  |
| **비밀번호 불일치**  | "비밀번호가 일치하지 않습니다."                    | 비밀번호와 비밀번호 확인 값이 다름 |
| **회원가입 진행 중** | 회원가입 버튼 비활성화 및 로딩 표시                  | 회원가입 버튼 클릭          |
| **회원가입 성공**   | "가입이 완료되었습니다. 로그인해주세요."  메시지 표시 후 로그인 화면(/login)으로 이동 | 회원가입 API 성공         |
| **회원가입 실패**   | "회원가입에 실패했습니다. 다시 시도해주세요."            | 회원가입 API 실패         |
| **이메일 중복-409**    | "이미 가입된 이메일입니다."  메시지 표시               | 서버에서 이메일 중복 응답      |
| **네트워크 오류**   | "네트워크 오류가 발생했습니다. 잠시 후 다시 시도해주세요."    | 서버 연결 실패            |

### 3.2.5 사용자 인터랙션 및 유효성

```text
[/login] → [회원가입하기] → [/register]
                              ↓
                        회원정보 입력
                              ↓
                       클라이언트 검증
                    ┌─────────┴─────────┐
                    ↓                   ↓
                  실패                 성공
                    ↓                   ↓
                필드 오류       POST /api/auth/register
                                      ↓
                             ┌────────┴────────┐
                             ↓                 ↓
                           200               409/400
                             ↓                 ↓
                        /login 이동      오류 표시
```

```text
이름          — 공백 아님, 1~50자
이메일        — 형식 일치, 최대 100자
비밀번호      — 공백 아님
비밀번호 확인 — password와 일치
성별          — 선택됨
출생년도      — 4자리 숫자, 1900 ~ 현재연도
```

> 회원가입 이후 사용자는 회원정보를 수정할 수 없으며, 마이페이지에서 조회 전용으로 표시한다. 관리자만 회원정보를 수정할 수 있다.

---

## 3.3 SCR-03 — KDSQ-P 1차 검사

### 3.3.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | KDSQ-P 1차 검사 |
| **URL** | `/surveys` |
| **화면 타입** | 입력 |
| **권한 레벨** | 로그인 필요 |
| **주요 기능** | 5문항 인지선별검사 및 점수에 따른 2차 검사 여부 결정 |
| **연결 API** | `GET /api/surveys/p`, `POST /api/exams/p` |

### 3.3.2 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│             KDSQ-P 인지선별검사 (1차)                    │
│       간단한 인지기능 자가검사를 시작합니다.             │
│                                                         │
│                  진행률 1 / 5                            │
│       ━━━━━━━━━━━━━━━━━━━━━━━━━━━                       │
│       Q1. 최근 일어난 일을 기억하는 데                  │
│           어려움이 있습니까?                             │
│                                                         │
│       ○ 전혀 그렇지 않다                                 │
│       ○ 가끔 그렇다                                      │
│       ○ 자주 그렇다                                      │
│                                                         │
│                   [이전] [다음]                          │
└─────────────────────────────────────────────────────────┘
```

### 3.3.3 UI 컴포넌트 매핑

| 화면 요소 | 컴포넌트            | 라이브러리 컴포넌트                  | 역할                          |
| ----- | --------------- | --------------------------- | --------------------------- |
| 상단 메뉴 | Header          | `Header` / `NavigationMenu` | 검사하기, 마이페이지, 통계, 로그아웃 메뉴 제공 |
| 검사 제목 | SurveyHeader    | `Card` / `Typography`       | KDSQ-P 검사명 및 검사 안내 문구 표시    |
| 진행률   | ProgressBar     | `Progress` (shadcn)         | 현재 문항 및 전체 문항 진행률 표시        |
| 문항 영역 | QuestionCard    | `Card` (shadcn)             | 현재 검사 문항 표시                 |
| 문항 번호 | QuestionNumber  | `Typography`                | 현재 문항 번호 표시                 |
| 문항 내용 | QuestionContent | `Typography`                | KDSQ-P 질문 내용 표시             |
| 답변 선택 | AnswerOptions   | `RadioGroup` (shadcn)       | 문항별 답변 선택                   |
| 이전 버튼 | PreviousButton  | `Button` (shadcn)           | 이전 문항으로 이동                  |
| 다음 버튼 | NextButton      | `Button` (shadcn)           | 답변 저장 후 다음 문항으로 이동          |
| 제출 버튼 | SubmitButton    | `Button` (shadcn)           | 마지막 문항 답변 완료 후 검사 제출  

### 3.3.4 화면 상태

| 상태             | 표시 내용                                     | 트리거                     |
| -------------- | ------------------------------- | ----------------------- |
| **초기 상태**      | KDSQ-P 검사 안내 및 1번 문항 표시                   | 검사 화면 진입                |
| **검사 문항 로딩 중** | 로딩 표시                                     | `GET /api/surveys/p` 호출 |
| **검사 진행 중**    | 현재 문항, 진행률, 답변 선택지 표시                     | 검사 데이터 로딩 완료            |
| **답변 선택**      | 선택한 답변 상태 유지                              | 답변 항목 선택                |
| **이전 문항 이동**   | 이전 문항 및 기존 답변 표시                          | 이전 버튼 클릭                |
| **다음 문항 이동**   | 다음 문항 및 진행률 갱신                            | 다음 버튼 클릭                |
| **입력 오류**      | 답변을 선택해주세요 메시지 표시                         | 답변 없이 다음/제출 버튼 클릭       |
| **제출 진행 중**    | 제출 버튼 비활성화 및 로딩 표시                        | 마지막 문항에서 제출 버튼 클릭       |
| **4점 미만**      | 정상 판정 및 결과 화면 표시                          | 검사 제출 결과 총점 4점 미만       |
| **4점 이상**      | KDSQ-C(2차) 상세검사 안내                        | 검사 제출 결과 총점 4점 이상       |
| **검사 제출 실패**   | 검사 제출 실패 메시지 표시                           | `POST /api/exams/p` 실패  |
| **네트워크 오류**    | "네트워크 오류가 발생했습니다. 잠시 후 다시 시도해주세요." 메시지 표시 | 서버 연결 실패  

### 3.3.5 사용자 인터랙션 흐름

```text
[검사 진입]
   → GET /api/surveys/p
   → 문항 표시
   → 답변 선택
   → 다음
      ├─ 미선택 → 오류
      └─ 선택 → 다음 문항
                 ↓
              5문항 완료
                 ↓
           POST /api/exams/p
                 ↓
          firstScore 산출
          ┌──────┴──────┐
          ↓             ↓
        0~3점          4~10점
          ↓             ↓
        정상       KDSQ-C 안내
          ↓             ↓
      /results/:id   /surveys/c
```

### 3.3.6 상태 및 데이터 정의 

| 상태 변수             | 타입                    | 설명                   |
| ----------------- | --------------------- | -------------------- |
| `questions`       | `Question[]`          | KDSQ-P 5개 검사 문항 목록   |
| `currentQuestion` | `number`              | 현재 문항의 인덱스 또는 번호     |
| `answers`         | `Answer[]` / `Record` | 각 문항에 대해 사용자가 선택한 답변 |
| `loading`         | `boolean`             | 검사 문항 조회 중 여부        |
| `submitting`      | `boolean`             | 검사 결과 제출 중 여부        |
| `totalScoreP`     | `number`              | KDSQ-P 전체 문항의 합산 점수  |

---

## 3.4 SCR-04 — KDSQ-C 2차 검사

### 3.4.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | KDSQ-C 2차 상세검사 |
| **URL** | `/surveys/c` |
| **화면 타입** | 입력 |
| **권한 레벨** | 로그인 필요 |
| **주요 기능** | KDSQ-P 4점 이상 사용자를 대상으로 15문항 상세검사 진행 |
| **연결 API** | `GET /api/surveys/c`, `POST /api/exams/c` |

### 3.4.2 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│                 KDSQ-C 상세검사                          │
│                                                         │
│                  진행률 7 / 15                           │
│       ━━━━━━━━━━━━━━━━━━━░░░░░░░                        │
│                                                         │
│       Q7. 현재 상황을 이해하고                           │
│           적절하게 대처하는 데                            │
│           어려움이 있습니까?                             │
│                                                         │
│       ○ 전혀 그렇지 않다                                 │
│       ○ 가끔 그렇다                                      │
│       ○ 자주 그렇다                                      │
│                                                         │
│                  [이전] [다음]                           │
└─────────────────────────────────────────────────────────┘
```

### 3.4.3 UI 컴포넌트 매핑

| 화면 요소 | 컴포넌트            | 라이브러리 컴포넌트                  | 역할                      |
| ----- | --------------- | --------------------------- | ----------------------- |
| 검사 제목 | SurveyHeader    | `Card` / `Typography`       | KDSQ-C 상세검사명 및 안내 문구 표시 |
| 진행률   | ProgressBar     | `Progress` (shadcn)         | 현재 문항 및 전체 15문항 진행률 표시  |
| 문항 영역 | QuestionCard    | `Card` (shadcn)             | 현재 검사 문항 표시             |
| 문항 번호 | QuestionNumber  | `Typography`                | 현재 문항 번호 표시             |
| 문항 내용 | QuestionContent | `Typography`                | KDSQ-C 질문 내용 표시         |
| 답변 선택 | AnswerOptions   | `RadioGroup` (shadcn)       | 현재 문항의 답변 선택            |
| 이전 버튼 | PreviousButton  | `Button` (shadcn)           | 이전 문항으로 이동              |
| 다음 버튼 | NextButton      | `Button` (shadcn)           | 다음 문항으로 이동              |
| 제출 버튼 | SubmitButton    | `Button` (shadcn)           | 15문항 완료 후 검사 결과 제출      |

### 3.4.4 문항 영역 및 점수 구성

KDSQ-C는 총 15문항으로 구성되며, 문항 번호에 따라 3개 영역으로 구분하여 점수를 집계한다.

| 영역                    | 문항    | 점수 필드         | 설명                   |
| --------------------- | ----- | ------------- | -------------------- |
| **기억력 (KDSQ-M)**      | 1~5   | `memoryScore` | 기억력 관련 문항 점수 합계      |
| **기타 인지기능 (KDSQ-O)**  | 6~10  | `otherScore`  | 기타 인지기능 관련 문항 점수 합계  |
| **일상생활수행능력 (KDSQ-A)** | 11~15 | `adlScore`    | 일상생활수행능력 관련 문항 점수 합계 |
| **전체**                | 1~15  | `totalScore`  | 3개 영역 점수의 합계         |

```text
memoryScore = Q1 + Q2 + Q3 + Q4 + Q5
otherScore  = Q6 + Q7 + Q8 + Q9 + Q10
adlScore    = Q11 + Q12 + Q13 + Q14 + Q15

totalScore = memoryScore + otherScore + adlScore
```

> 문항별 `answers`는 저장하지 않는다. `memoryScore`, `otherScore`, `adlScore`를 백엔드에 저장하고 `totalScore`는 프론트에서 계산한다.

### 3.4.5 화면 상태

| 상태              | 표시 내용                                     | 트리거                     |
| --------------- | ----------------------------------------- | ----------------------- |
| **초기 상태**       | KDSQ-C 상세검사 및 1번 문항 표시                    | 2차 검사 화면 진입             |
| **검사 문항 로딩 중**  | 로딩 표시                                     | `GET /api/surveys/c` 호출 |
| **검사 진행 중**     | 현재 문항, 진행률, 답변 선택지 표시                     | 검사 데이터 로딩 완료            |
| **답변 선택**       | 선택한 답변 상태 표시                              | 답변 항목 선택                |
| **이전 문항 이동**    | 이전 문항 및 진행률 표시                            | 이전 버튼 클릭                |
| **다음 문항 이동**    | 다음 문항 및 진행률 갱신                            | 다음 버튼 클릭                |
| **입력 오류**       | "답변을 선택해주세요." 메시지 표시                      | 답변 없이 다음/제출 버튼 클릭       |
| **제출 진행 중**     | 제출 버튼 비활성화 및 로딩 표시                        | 마지막 문항에서 제출 버튼 클릭       |
| **결과 계산 중**     | 영역별 점수 및 총점 계산                            | `POST /api/exams/c` 호출  |
| **주의**          | 주의 판정 결과 표시                               | `totalScore < 6`        |
| **고위험군**      | 고위험군 판정 결과 표시                      | `totalScore ≥ 6`        |
| **제출 실패**       | 검사 제출 실패 메시지 표시                           | `POST /api/exams/c` 실패  |
| **네트워크 오류**     | "네트워크 오류가 발생했습니다. 잠시 후 다시 시도해주세요." 메시지 표시 | 서버 연결 실패 

### 3.4.6 상태 및 데이터 정의

| 상태 변수             | 타입                    | 설명                                    |
| ----------------- | --------------------- | ------------------------------------- |
| `questions`       | `Question[]`          | KDSQ-C 15개 검사 문항 목록                   |
| `currentQuestion` | `number`              | 현재 문항 번호                              |
| `answers`         | `Answer[]` / `Record` | 검사 진행 중 화면에서 사용하는 답변 상태               |
| `loading`         | `boolean`             | 검사 문항 조회 중 여부                         |
| `submitting`      | `boolean`             | 검사 결과 제출 중 여부                         |
| `memoryScore`     | `number`              | 1~5번 문항의 기억력 영역 점수                    |
| `otherScore`      | `number`              | 6~10번 문항의 기타 인지기능 영역 점수               |
| `adlScore`        | `number`              | 11~15번 문항의 일상생활수행능력 영역 점수             |
| `totalScore`      | `number`              | `memoryScore + otherScore + adlScore` |

> **데이터 저장 정책**: 문항별 개별 응답(answers)은 저장하지 않고, `memoryScore` / `otherScore` / `adlScore` / `totalScore` 만 `ExamResult`에 저장한다. 이 점수를 기반으로 영역별 통계(Admin 대시보드)를 제공한다

### 3.4.7 사용자 인터랙션 흐름

```text
[KDSQ-P 4점 이상]
   → KDSQ-C 안내
   → GET /api/surveys/c
   → 15문항 진행
   → 마지막 문항 제출
   → POST /api/exams/c
   → 영역별 점수 산출
   → totalScore 계산
      ┌──────────────┴──────────────┐
      ↓                             ↓
    0~5점                         6~30점
      ↓                             ↓
     주의                         고위험군
      └──────────────┬──────────────┘
                     ↓
              결과 화면 이동
```

---

## 3.5 SCR-05 — 검사 결과 화면

### 3.5.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 검사 결과 |
| **URL** | `/results/:examId` |
| **화면 타입** | 상세 |
| **권한 레벨** | 로그인 필요 |
| **주요 기능** | 검사 점수, 판정, KDSQ-C 영역별 점수 및 관리 안내 표시 |
| **연결 API** | `GET /api/exams/:examId` |
| **연결 화면** | 마이페이지(`/mypage`) |

### 3.5.2 KDSQ-P 정상 결과 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│                    검사 결과                             │
│                                                         │
│                 정상 범위입니다                          │
│                  총점 3점 / 10점                         │
│                                                         │
│       검사일시 : 2026-09-22                              │
│                                                         │
│                  [검사 이력 보기]                         │
└─────────────────────────────────────────────────────────┘
```

### 3.5.3 KDSQ-C 주의 결과 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│                    검사 결과                             │
│                  주의가 필요합니다                       │
│                  총점 5점                                │
│                                                         │
│ 기억력                2점                                │
│ 기타 인지기능          2점                                │
│ 일상생활수행능력        1점                                │
│                                                         │
│ 관리 및 추가 검진 관련 안내                              │
│                  [검사 이력 보기]                         │
└─────────────────────────────────────────────────────────┘
```

### 3.5.4 KDSQ-C 고위험군 결과 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│                    검사 결과                            │
│                  고위험군 의심                          │
│                  총점  12점                             │
│       ┌────────────────────────────────┐                │
│       │ 기억력             4점         │                │
│       │ 기타 인지기능       4점         │                │
│       │ 일상생활수행능력     4점         │                │
│       └────────────────────────────────┘                │
│       ───── 권장 안내 ─────                              │
│       인지 건강을 위한 관리 정보                         │
│       및 추가적인 검진 관련 안내를 확인하세요.            │
│       [맞춤 솔루션 확인]                                 │
│       [검사 이력 보기]                                   │
└─────────────────────────────────────────────────────────┘
```

### 3.5.5 UI 컴포넌트 매핑

| 화면 요소     | 컴포넌트           | 라이브러리 컴포넌트                  | 역할                           |
| --------- | -------------- | --------------------------- | ---------------------------- |
| 결과 제목     | ResultHeader   | `Typography`                | 검사 결과 제목 표시                  |
| 결과 상태     | ResultStatus   | `Badge` / `Alert`           | 정상, 주의, 고위험군 결과 표시    |
| 총점          | TotalScore     | `Card` / `Typography`       | 검사 총점 및 총점 기준 표시             |
| 영역별 점수    | ScoreSummary   | `Card` / `Progress`         | 기억력, 기타 인지기능, 일상생활수행능력 점수 표시 |
| 검사일시      | ExamDate       | `Typography`                | 검사 완료 일시 표시                  |
| 권장 안내     | Recommendation | `Alert` / `Card`            | 결과에 따른 건강관리 및 추가 검진 안내       |
| 맞춤 솔루션 버튼 | SolutionButton | `Button` (shadcn)           | 맞춤 솔루션 화면으로 이동               |
| 검사 이력 버튼  | HistoryButton  | `Button` (shadcn)           | 검사 이력 화면으로 이동

### 3.5.6 화면 상태 및 판정 기준

| 검사 | 점수 | 결과 | 화면 표시 |
|---|---:|---|---|
| KDSQ-P | 0~3 | 정상 | "정상 범위입니다" |
| KDSQ-P | 4~10 | 2차 대상 | 최종 결과 화면에서는 2차 검사 결과를 표시 |
| KDSQ-C | 0~5 | 주의 | "주의가 필요합니다" |
| KDSQ-C | 6~30 | 고위험군 | "고위험군 / 치매등급 의심" |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 결과 로딩 중 | 로딩 표시 | 결과 화면 진입 |
| 결과 정상 | 정상 문구 + 총점 | KDSQ-P 0~3 |
| 결과 주의 | 주의 문구 + 영역별 점수 | KDSQ-C 0~5 |
| 결과 고위험 | 고위험 문구 + 영역별 점수 + 관리 안내 | KDSQ-C 6~30 |
| 조회 실패 | 오류 메시지 + 재시도 | 결과 API 실패 |

> 관리 안내는 별도 `/solutions` 화면으로 분리하지 않고 결과 화면 안에서 제공한다. 상위 핵심 흐름에 맞춰 KDSQ-C의 주의/고위험군 결과 모두에 관리·추가 검진 관련 안내를 제공한다.

### 3.5.7 사용자 인터랙션 흐름

```text
[검사 제출 완료]
   → /results/:examId
   → GET /api/exams/:examId
   → 검사 종류 확인
      ├─ KDSQ-P → 총점/정상 결과 표시
      └─ KDSQ-C → 영역별 점수/총점/판정/관리 안내 표시

[검사 이력 보기]
   → /mypage
```
### 3.5.8 버튼 인터랙션

| 버튼            | 조건                     | 동작               |
| ------------- | ---------------------- | ---------------- |
| **검사 이력 보기**  | 모든 결과 화면               | `/history`로 이동   |
| **맞춤 솔루션 확인** | KDSQ-C 6~30점           | `/solutions`로 이동 |
| **맞춤 솔루션 확인** | KDSQ-P 정상 또는 KDSQ-C 주의 | 표시하지 않음          |

### 3.5.9 결과 문구 정책

검사 결과는 의료적 진단 또는 확진 결과로 표현하지 않는다. 화면에서는 다음 표현을 기준으로 사용한다.

| 결과         | 화면 표시 문구           |
| ---------- | ------------------ |
| KDSQ-P 정상  | **정상 범위입니다**       |
| KDSQ-C 주의  | **주의가 필요합니다**      |
| KDSQ-C 고위험 | **고위험군 / 치매등급 의심** |

---

## 3.6 SCR-06 — 마이페이지

### 3.6.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 마이페이지 |
| **URL** | `/mypage` |
| **화면 타입** | 목록/상세 |
| **권한 레벨** | 로그인 필요 |
| **주요 기능** | 회원정보 조회, 검사 이력, 검사 결과 추이 |
| **연결 API** | `GET /api/members/me`, `GET /api/exams/history` |

### 3.6.2 와이어프레임

```text
┌─────────────────────────────────────────────────────────┐
│ Header                                                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                      마이페이지                          │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 회원정보                                             │ │
│ │ ─────────────────────────────────────────────────── │ │
│ │  이름        홍길동                                  │ │
│ │  이메일      hong@example.com                        │ │
│ │  성별        남성                                    │ │
│ │  출생년도    1960 (만 66세)                          │ │
│ │                                                     │ │
│ │ ℹ️ 회원정보 변경이 필요하시면 관리자에게 문의해주세요. │ │  ← ReadOnlyNotice
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ 검사 이력                                     총 3건     │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 검사일        검사      총점    판정      상세        │ │
│ │ ─────────────────────────────────────────────────── │ │
│ │ 2026-09-22   KDSQ-C     5     [ 주의 ]   보기 >     │ │
│ │ 2026-06-20   KDSQ-C     7     [ 위험 ]   보기 >     │ │
│ │ 2026-03-10   KDSQ-P     3     [ 정상 ]   보기 >     │ │
│ └─────────────────────────────────────────────────────┘ │
│                                         < 1 2 3 >       │
│                                                         │
│ 검사 결과 추이                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 점수                                                 │ │
│ │  10│                                                 │ │
│ │   8│              ●────                              │ │
│ │   6│         ────╱     ╲                             │ │
│ │   4│   ●────╱           ╲────●                       │ │
│ │   2│                                                 │ │
│ │   0└──────────────────────────────────── 검사일      │ │
│ │      03-10      06-20      09-22                     │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.6.3 UI 컴포넌트 매핑

| 화면 요소    | 컴포넌트               | 라이브러리 컴포넌트       | 역할                                            |
| :------- | :----------------- | :--------------- | :-------------------------------------------- |
| 마이페이지    | `MyPage`           | -                | 화면 컨테이너, 회원정보 및 검사 이력 데이터 조회, 로딩·빈 상태 등 상태 분기 |
| 회원정보 영역  | `ProfileCard`      | `Card` (shadcn)  | 이름·이메일·성별·출생년도 표시 (읽기 전용)                     |
| 회원정보 안내  | `ReadOnlyNotice`   | `Alert` (shadcn) | "회원정보 변경이 필요하시면 관리자에게 문의해주세요." 안내             |
| 검사 이력 영역 | `ExamHistoryTable` | `Table` (shadcn) | 검사일·검사유형·총점·판정 목록 표시 및 페이지네이션                 |
| 검사 결과    | `RiskBadge`        | `Badge` (shadcn) | 판정 등급을 Chip 형태로 표시 (정상/주의/위험)                 |
| 검사 추이    | `ExamTrendChart`   | `LineChart`      | 검사일 기준 총점 시계열 그래프 표시                          |
| 검사 이력 없음 | `EmptyHistory`     | `Card` / `Alert` | 검사 이력이 0건인 경우 안내 및 검사 시작 버튼 제공  

### 3.6.4 화면 상태 목록

| 상태              | 표시 내용                                     | 트리거                         |
| --------------- | ----------------------------------------- | --------------------------- |
| **초기 상태**       | 회원정보 및 검사 이력 영역 표시                        | 마이페이지 진입                    |
| **회원정보 로딩 중**   | 회원정보 Skeleton 표시                          | `GET /api/members/me` 호출    |
| **검사 이력 로딩 중**  | 검사 이력 Skeleton 표시                         | `GET /api/exams/history` 호출 |
| **조회 완료**       | 회원정보 및 검사 이력 표시                           | API 호출 성공                   |
| **검사 이력 없음**    | "검사 결과가 없습니다." 안내                         | 검사 이력 데이터가 없는 경우            |
| **검사 이력 있음**    | 검사 결과 목록 및 추이 그래프 표시                      | 검사 이력 데이터 존재                |
| **결과 유형 표시**    | 정상 / 주의 / 고위험군                                 | 검사 결과 데이터 로딩 완료             |
| **회원정보 조회 실패**  | 회원정보 조회 실패 메시지 표시                         | `GET /api/members/me` 실패    |
| **검사 이력 조회 실패** | 검사 이력 조회 실패 메시지 표시                        | `GET /api/exams/history` 실패 |
| **네트워크 오류**     | "네트워크 오류가 발생했습니다. 잠시 후 다시 시도해주세요." 메시지 표시 | 서버 연결 실패                    |
| **로그아웃**        | 로그아웃 처리 후 로그인 화면 이동                       | 로그아웃 클릭                     |

### 3.6.5 상태 및 데이터 정의 

| 상태 변수         | 타입             | 설명                |
| ------------- | -------------- | ----------------- |
| `member`      | `Member`       | 로그인한 사용자의 회원정보    |
| `examHistory` | `ExamResult[]` | 사용자의 과거 검사 결과 목록  |
| `loading`     | `boolean`      | 데이터 조회 중 여부       |
| `error`       | `string`       | 에러                     |

**회원정보**
```text
member
├── name
├── email
├── gender
└── birthYear
```

**검사 이력**
```text
examHistory[]
├── examId
├── examType
├── totalScore
├── resultType
└── examDate
```

**KDSQ-C 영역별 데이터**
```text
KDSQ-C
├── memoryScore
├── otherScore
├── adlScore
└── totalScore
```

> 문항별 answers는 마이페이지에서 조회하거나 표시하지 않는다.

### 3.6.6 검사 이력 표시 기준

| 항목     | 내용               |
| ------ | ---------------- |
| **날짜** | 검사 완료일           |
| **검사** | KDSQ-P 또는 KDSQ-C |
| **총점** | 해당 검사 총점         |
| **결과** | 검사 점수에 따른 결과 유형  |

**결과 유형**

| 검사     |   점수 | 표시 결과       |
| ------ | ---: | ----------- |
| KDSQ-P |  0~3 | 정상          |
| KDSQ-P | 4~10 | 2차 진행       |
| KDSQ-C |  0~5 | 주의          |
| KDSQ-C | 6~30 | 고위험군      |

### 3.6.7 표시 규칙

| 원본 값 | 화면 표기 |
| :--- | :--- |
| `gender: MALE / FEMALE` | 남성 / 여성 |
| `birthYear: 1960` | 1960 (만 66세) — 현재연도 − 출생년도 |
| `examType: KDSQ_P / KDSQ_C` | KDSQ-P / KDSQ-C |
| `riskLevel: Normal` | `정상` Chip |
| `riskLevel: Borderline` | `주의` Chip |
| `riskLevel: HighRisk` | `위험` Chip |
| `createdAt` | `YYYY-MM-DD` (시각 생략) |

### 3.6.8 사용자 인터랙션 흐름

```text
[마이페이지 클릭]
   ↓
[ProtectedRoute 통과 확인 (토큰 유무)]
   ↓
[MyPage 마운트] → loading = true
   ↓
[GET /api/members/me]        ─┐
[GET /api/exams/history]     ─┴─ 병렬 호출 (Promise.all)
   → [두 요청 모두 성공]
          → [calcTotalScore()로 이력별 총점 합산 (서버는 영역 점수만 반환)]
                 ↓
         [examHistory.length === 0 ?]
              → [Yes] → [ProfileCard + 빈 상태 안내 (그래프 미렌더)]
              → [No]  → [ProfileCard + ExamHistoryTable + ExamTrendChart]

   → [401] → [토큰 만료] → [3.4 재발급 플로우]

   → [그 외 오류] → [Alert + 다시 시도 버튼]
```

**토큰 만료 / 재발급**

```text
[API 응답 401 수신 (axios 응답 인터셉터)]
   ↓
[리프레시 토큰 보유?]
   │
   ├── [Yes] → [POST /api/auth/reissue]
   │            ├── [성공] → [새 액세스 토큰 저장] → [실패했던 요청 1회 재시도]
   │            └── [실패] → [authStore.logout()] → [/login ("다시 로그인해주세요.")]
   │
   └── [No]  → [authStore.logout()] → [/login]
```

**로그아웃**

```text
[로그아웃 클릭]
   ↓
[POST /api/auth/logout   (서버 리프레시 토큰 폐기)]
   ↓
[authStore.logout()      (액세스·리프레시 토큰 및 memberStore 초기화)]
   ↓
[/login 으로 replace 이동 (뒤로가기로 복귀 불가)]
```

> 액세스 토큰은 stateless이므로 클라이언트 삭제가 실질적인 로그아웃이며,
> 서버 호출은 리프레시 토큰 재사용을 막기 위한 것이다. API 실패 시에도 클라이언트 상태는 반드시 초기화한다.

### 3.6.9 회원 정보 조회 정책

회원정보는 마이페이지에서 **조회 전용**으로 제공한다.

**수정 정책**

| 회원정보 | 마이페이지 수정 |
| ---- | -------- |
| 이름   | ❌        |
| 이메일  | ❌        |
| 성별   | ❌        |
| 출생년도 | ❌        |

### 3.6.10 빈 상태

사용자가 아직 검사를 완료하지 않은 경우 다음과 같이 표시한다.

```text
┌─────────────────────────────────────────────────────────┐
│ 최근 검사 결과                                          │
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │                                                     │ │
│ │            아직 검사 이력이 없습니다.                  │ │
│ │                                                     │ │
│ │          ┌──────────────────────┐                   │ │
│ │          │   검사 시작하기 →     │                   │ │
│ │          └──────────────────────┘                   │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ (검사 결과 추이 섹션은 렌더링하지 않음)                     │
└─────────────────────────────────────────────────────────┘
```

**검사 시작하기** 클릭 시 `/surveys/p`로 이동한다.

---

# 4. 관리자 화면별 상세 설계

## 4.1 ADM-01 — 관리자 로그인

### 4.1.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 관리자 로그인 |
| **URL** | `/admin/login` |
| **화면 타입** | 입력 |
| **권한 레벨** | 공개 |
| **주요 기능** | 사전 등록 관리자 계정으로 로그인 |
| **연결 처리** | Spring Security `formLogin` |

### 4.1.2 와이어프레임

```text
┌──────────────────────────────────────────────┐
│               KDSQ 관리자                    │
│                                              │
│ 아이디                                        │
│ [________________________________]           │
│ 비밀번호                                      │
│ [________________________________]           │
│                                              │
│ [                 로그인                 ]   │
│                                              │
│ 사전 등록된 관리자 계정만 사용할 수 있습니다.  │
│ 계정 발급은 시스템 담당자에게 문의하세요.      │
└──────────────────────────────────────────────┘
```

### 4.1.3 UI 컴포넌트 및 상태

| 화면 요소 | 구성 | 역할 |
|---|---|---|
| 로그인 폼 | Thymeleaf form | 관리자 인증 |
| 아이디/비밀번호 | Input | 계정 입력 |
| 로그인 버튼 | Button | `formLogin` 제출 |
| 오류 메시지 | Alert/text | 인증 실패 안내 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 로그인 폼 | 진입 |
| 로그인 실패 | "아이디 또는 비밀번호가 올바르지 않습니다." | `?error` |
| 로그인 성공 | `/admin/dashboard` 이동 | 인증 성공 |
| 세션 만료 | 로그인 화면 | 보호된 관리자 화면 접근 |

### 4.1.4 처리 흐름

```text
아이디/비밀번호 입력
   → 로그인
   → Spring Security formLogin
      ├─ 실패 → /admin/login?error
      └─ 성공 → 세션 생성 → /admin/dashboard
```

---

## 4.2 ADM-02 — 관리자 대시보드

### 4.2.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 대시보드 |
| **URL** | `/admin/dashboard` |
| **화면 타입** | 대시보드 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 활성 회원/검사 현황, 위험도 분포, 종합·영역별 평균, 월별 추이 |
| **연결 처리** | `AdminDashboardController → AdminStatisticsService` |

### 4.2.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ Header                                                    │
├───────────┬───────────────────────────────────────────────┤
│ 대시보드   │                대시보드                        │
│ 회원관리   │                                               │
│ 검사결과   │  전체 회원 수(활성)   전체 검사 건수(활성)       │
│           │      128명               342건                │
│           │                                               │
│           │  위험도 분포                                   │
│           │  정상 45%   주의 30%   고위험군 25%             │
│           │                                               │
│           │  종합점수 평균           8.4점                 │
│           │  영역별 평균                                   │
│           │   기억력(Memory)      3.1점                   │
│           │   기타인지(Other)     2.8점                   │
│           │   ADL                2.5점                   │
│           │                                               │
│           │  월별 평균 점수 추이 / 월별 검사 건수 추이       │
│           │  최근 고위험군 검사 목록 (바로가기)              │
└───────────┴───────────────────────────────────────────────┘
```

### 4.2.3 UI 컴포넌트 및 상태

| 화면 요소      | Thymeleaf 요소                  | 역할                           |
| ---------- | --------------------- | ---------------------------- |
| 사이드바       | `layout:fragment="content"` 좌측 nav | 대시보드/회원관리/검사결과 메뉴 |
| 전체 회원 수    | `th:text="${stats.totalMembers}"`  | 활성 회원 수 표시                |
| 전체 검사 건수   | `th:text="${stats.totalExams}"`    | 활성 검사 건수 표시               |
| 위험도 분포     | `th:each` + 배지/차트         | 정상·주의·고위험군 분포 표시             |
| 종합/영역별 점수 평균 | `th:text`         | KDSQ-C 평균 점수 표시              |
| 월별 추이 차트   | Chart.js 등             | 월별 평균 점수·검사 건수 추이            |
| 최근 고위험군 목록 | `th:each="e : ${stats.recentHighRisk}"` | 최근 고위험군 검사 결과 표시 

| 상태 | 표시 내용 | 트리거 |
| 상태             | 표시 내용                              | 트리거                |
| -------------- | ---------------------------------- | ------------------ |
| **초기 상태**      | 관리자 대시보드 기본 레이아웃 표시                | `/admin/dashboard` 진입 |
| **데이터 없음**     | "검사 결과가 없습니다." 표시                  | 활성 검사 결과 0건       |
| **회원관리 이동**    | 회원관리 화면으로 이동                       | 회원관리 메뉴 클릭         |
| **검사결과 이동**    | 검사결과 화면으로 이동                       | 검사결과 메뉴 또는 전체보기 클릭 |
| **로그아웃**       | 로그아웃 처리 후 로그인 화면 이동                | 로그아웃 클릭            |

> 영역별 통계는 SCR-04에서 정의한 memoryScore, otherScore, adlScore를 기반으로 집계한다.

### 4.2.4 처리 흐름

```text
[대시보드 요청] → AdminDashboardController → AdminStatisticsService
    → [활성 회원 수 / 활성 검사 건수 조회]
    → [위험도 분포 집계 (active=true)]
    → [종합·영역별 평균 집계 (active=true)]
    → [월별 추이 집계 (active=true)]
    → Model 주입 → dashboard.html 렌더링
```

---

## 4.3 ADM-03 — 회원 목록

### 4.3.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 회원 관리(목록) |
| **URL** | `/admin/members` |
| **화면 타입** | 목록 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 이름/이메일 검색, 회원 상태 확인, 상세 이동 |
| **연결 처리** | `AdminMemberController.list → AdminMemberService.getMembers` |

### 4.3.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ 회원 관리                                                 │
│ [검색: 이름/이메일]                              [검색]    │
├───────────────────────────────────────────────────────────┤
│ 이름   이메일             성별  출생년도  가입일   상태 [상세]│
│ 홍길동 hong@example.com   남성  1960     2026-01-10 활성 [보기]│
│ 김영희 kim@example.com    여성  1955     2026-02-03 비활성[보기]│
├───────────────────────────────────────────────────────────┤
│                     ◀ 1 2 3 ... ▶                         │
└───────────────────────────────────────────────────────────┘
```

### 4.3.3 UI 컴포넌트 및 상태

| 상태 | 표시 내용 | 트리거 |
| --- | --- | --- |
| **초기 상태** | 전체 회원 목록(페이징) 표시 | `/admin/members` 진입 |
| **검색 결과 있음** | 검색 조건에 맞는 회원 표시 | 검색 버튼 클릭 |
| **검색 결과 없음** | "검색 결과가 없습니다." | 조건에 맞는 회원 0건 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 회원 목록 | 진입 |
| 검색 결과 있음 | 조건에 맞는 회원 | 검색 |
| 검색 결과 없음 | "검색 결과가 없습니다." | 결과 0건 |

### 4.3.4 처리 흐름

```text
[회원 목록 요청] → AdminMemberController.list
    → AdminMemberService.getMembers(keyword, pageable)  // 이름/이메일 LIKE 검색 + 페이징
    → members/list.html 렌더링
```

---

## 4.4 ADM-04 — 회원 상세

### 4.4.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 회원 상세 |
| **URL** | `/admin/members/{id}` |
| **화면 타입** | 상세 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 회원정보 및 검사 이력 조회, 회원정보 수정, 계정 비활성화 |
| **연결 처리** | `AdminMemberController.detail` |

### 4.4.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ 회원 상세                           [수정] [계정 비활성화] │
│ 이름       홍길동                                         │
│ 이메일     hong@example.com                               │
│ 성별       남성                                           │
│ 출생년도   1960                                           │
│ 가입일     2026-01-10                                     │
│ 상태       활성                                           │
│                                                           │
│ 검사 이력 (총 3건)                                        │
│ 날짜        KDSQ-P       KDSQ-C          [상세]           │
│ 2026-09-22  4점/2차대상   5점/주의        [보기]           │
│ 2026-06-20  6점/2차대상   9점/고위험군     [보기]           │
│ 2026-03-10  2점/정상      —              [보기]           │
└───────────────────────────────────────────────────────────┘
```

### 4.4.3 UI 컴포넌트 및 상태

| 화면 요소 | 구성 | 역할 |
|---|---|---|
| 회원정보 | Thymeleaf text | 회원정보 표시 |
| 수정 버튼 | Link/Button | `/edit` 이동 |
| 비활성화 버튼 | Button | 비활성화 확인 |
| 검사 이력 | Table | 검사 건수 및 P/C 결과 요약 |
| 상세 버튼 | Link/Button | `/admin/exams/{id}` 이동 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 회원정보 + 검사 이력 | 진입 |
| 검사 이력 없음 | "검사 이력이 없습니다." | 이력 0건 |
| 비활성화 확인 | 비활성화 확인 문구 | 버튼 클릭 |
| 비활성화 완료 | 상태를 비활성으로 갱신 | 처리 성공 |

### 4.4.4 처리 흐름

```text
회원 상세 요청
   → 회원정보 + 검사이력 조회
   → detail.html 렌더링

[수정]
   → /admin/members/{id}/edit

[계정 비활성화]
   → 확인
   → POST /admin/members/{id}/deactivate
   → Member.status = INACTIVE
   → 상세 화면 상태 갱신
```

> 계정 비활성화는 물리 삭제가 아닌 상태값 변경으로 처리하며, 연결된 검사 결과는 보존한다.

---

## 4.5 ADM-04-1 — 회원 정보 수정

### 4.5.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 회원 정보 수정 |
| **URL** | `/admin/members/{id}/edit` |
| **화면 타입** | 입력 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 이름/이메일/성별/출생년도 수정 |
| **연결 처리** | `editForm(GET)` / `update(POST)` |

### 4.5.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ 회원 정보 수정                                             │
│                                                           │
│ 이름       [ 홍길동                    ]                  │
│ 이메일     [ hong@example.com          ]                  │
│ 성별       ○ 남성   ○ 여성                                │
│ 출생년도   [ 1960 ]                                       │
│ 가입일     2026-01-10 (읽기 전용)                          │
│ 상태       활성       (읽기 전용)                          │
│                                                           │
│              [취소]        [저장]                          │
└───────────────────────────────────────────────────────────┘
```

### 4.5.3 UI 컴포넌트 및 상태

| 화면 요소 | 구성 | 역할 |
|---|---|---|
| 입력 필드 | Input/Radio | 회원정보 수정 |
| 가입일/상태 | 읽기 전용 text | 수정 불가 정보 표시 |
| 저장/취소 | Button | 저장 또는 상세로 이동 |
| 오류 메시지 | helperText/Alert | 검증 오류 표시 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 기존 회원정보가 채워진 폼 | 진입 |
| 입력 오류 | 필드별 오류 | 검증 실패 |
| 저장 성공 | 수정 완료 후 회원 상세 이동 | 성공 |
| 저장 실패 | 오류 메시지 + 입력값 유지 | 실패 |

### 4.5.4 처리 흐름

```text
수정 화면 진입
   → GET /admin/members/{id}/edit
   → 기존 값 표시
   → 저장
   → 유효성 검증
      ├─ 실패 → 오류 표시
      └─ 성공 → POST /admin/members/{id}/edit
                → /admin/members/{id} 이동
```

---

## 4.6 ADM-05 — 검사 결과 목록

### 4.6.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 검사 결과 관리(목록) |
| **URL** | `/admin/exams` |
| **화면 타입** | 목록 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 검사종류/위험도/이름/기간 필터, 상세 조회, 결과 비활성화 |
| **연결 처리** | `AdminExamController.list → AdminExamService.search` |

### 4.6.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ 검사 결과 관리                                             │
│ [검사종류▼] [정상][주의][고위험군] [이름검색] [날짜~날짜] [검색] │
├───────────────────────────────────────────────────────────┤
│ 날짜        회원   KDSQ-P       KDSQ-C        위험도  작업   │
│ 2026-09-22  홍길동  4점/2차대상   5점/주의      주의   [보기][삭제]│
│ 2026-09-21  김영희  6점/2차대상   9점/고위험군   위험   [보기][삭제]│
└───────────────────────────────────────────────────────────┘
```

### 4.6.3 UI 컴포넌트 및 상태

| 화면 요소 | 구성 | 역할 |
|---|---|---|
| 검사종류 필터 | Select | KDSQ-P/C 필터 |
| 위험도 필터 | Checkbox | 정상/주의/고위험군 필터 |
| 이름/기간 검색 | Input/Date | 검색 조건 |
| 결과 목록 | Table | 검사 결과 요약 |
| 보기 버튼 | Link/Button | 검사 상세 이동 |
| 삭제 버튼 | Button | 결과 비활성화 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 활성 검사 결과 목록 | 진입 |
| 필터 적용 | 조건에 맞는 결과 | 검색 |
| 결과 없음 | "검색 결과가 없습니다." | 결과 0건 |
| 삭제 확인 | 삭제 확인 문구 | 삭제 클릭 |
| 삭제 완료 | 목록에서 제거 + 완료 안내 | 처리 성공 |

### 4.6.4 처리 흐름

```text
검사 결과 목록 요청
   → 활성(active=true) 결과 조회
   → 필터/검색 적용
   → 목록 렌더링

[삭제]
   → 확인
   → POST /admin/exams/{id}/delete
   → ExamResult.active = false
   → 목록에서 제외
```

> 화면의 "삭제"는 물리 삭제가 아니라 비활성화(soft delete)를 의미한다. 비활성 결과는 목록과 통계에서 제외한다.

---

## 4.7 ADM-06 — 검사 상세 결과

### 4.7.1 화면 개요

| 항목 | 내용 |
|---|---|
| **화면명** | 검사 상세 결과 |
| **URL** | `/admin/exams/{id}` |
| **화면 타입** | 상세 |
| **권한 레벨** | 관리자 전용 |
| **주요 기능** | 점수/판정/관리 안내 조회, 회원 상세 연결, 결과 비활성화 |
| **연결 처리** | `AdminExamController.detail → AdminExamService.getDetail` |

### 4.7.2 와이어프레임

```text
┌───────────────────────────────────────────────────────────┐
│ 검사 상세 결과                                    [삭제]   │
│ 회원         홍길동 (hong@example.com)  → [회원 상세 보기] │
│ 검사종류     KDSQ-C     검사일시   2026-09-22              │
│                                                           │
│ 기억력(memoryScore)        2점                            │
│ 기타인지(otherScore)       2점                            │
│ ADL(adlScore)              1점                            │
│ 총점(totalScore)           5점                            │
│ 위험도                      주의                           │
│                                                           │
│ ───── 관리 및 추가 검진 안내 (읽기 전용) ─────              │
│ 인지 건강을 위한 관리 정보 안내 텍스트 노출                 │
└───────────────────────────────────────────────────────────┘
```

### 4.7.3 UI 컴포넌트 및 상태

| 화면 요소 | 구성 | 역할 |
|---|---|---|
| 회원 정보 | Thymeleaf text + Link | 회원 상세 연결 |
| 점수 정보 | Thymeleaf text | 영역별 점수/총점 |
| 위험도 | Badge/text | 판정 표시 |
| 관리 안내 | Read-only text | 안내 문구 |
| 삭제 버튼 | Button | 결과 비활성화 |

| 상태 | 표시 내용 | 트리거 |
|---|---|---|
| 초기 | 검사 상세 데이터 | 진입 |
| 삭제 확인 | 확인 문구 | 삭제 클릭 |
| 삭제 완료 | 완료 안내 후 목록 이동 | 처리 성공 |

### 4.7.4 처리 흐름

```text
상세 조회
   → 검사 결과 + 회원 정보 조회
   → detail.html 렌더링

[회원 상세 보기]
   → /admin/members/{memberId}

[삭제]
   → 확인
   → POST /admin/exams/{id}/delete
   → active=false
   → /admin/exams 이동
```

> 솔루션/관리 안내는 별도 관리 화면 없이 검사 상세 결과의 읽기 전용 영역으로 제공한다.

---

# 5. 네비게이션 흐름도

```text
[로그인 /login]
   ├── [회원가입 /register]
   │       └── 가입 성공 → /login
   │
   └── 로그인 성공 → [KDSQ-P /surveys]
                         │
                         ├── 0~3점 → [결과 /results/:examId]
                         │
                         └── 4~10점 → [KDSQ-C /surveys/c]
                                         ↓
                                   [결과 /results/:examId]
                                         ↓
                                  [마이페이지 /mypage]
                                         ↑
                               검사 이력 [상세보기]

[마이페이지 /mypage]
   └── 검사 시작하기 → /surveys

[관리자 로그인 /admin/login]
   └── [대시보드 /admin/dashboard]
          ├── [회원 목록 /admin/members]
          │      └── [회원 상세 /admin/members/{id}]
          │             ├── [회원 수정 /admin/members/{id}/edit]
          │             └── [검사 상세 /admin/exams/{id}]
          │
          └── [검사 결과 목록 /admin/exams]
                 └── [검사 상세 /admin/exams/{id}]
```

---

# 6. 디자인 토큰

## 6.1 색상

| 토큰 | 용도 | 기본값 |
|---|---|---|
| Primary | 주요 버튼/링크 | `#3b82f6` |
| Success | 정상/성공 | `#22c55e` |
| Warning | 주의 | `#f59e0b` |
| Danger | 오류/위험 | `#ef4444` |
| Neutral | 배경/테두리/보조 텍스트 | `#64748b` |

## 6.2 타이포그래피

| 용도 | 크기 | 굵기 |
|---|---:|---|
| 페이지 제목 | 24px | Bold |
| 섹션 제목 | 20px | SemiBold |
| 본문 | 16px | Normal |
| 보조 텍스트 | 14px | Normal |

## 6.3 간격

```text
컴포넌트 내부 패딩 : 16px
섹션 간격          : 24px
카드 패딩          : 24px
버튼 사이 간격     : 8px
폼 필드 간격       : 16px
```

---

# 7. 작성 체크리스트

- [x] 전체 사용자/관리자 화면 목록 작성
- [x] 화면별 URL과 권한 통일
- [x] 사용자/관리자 공통 레이아웃 정의
- [x] 각 화면별 와이어프레임 작성
- [x] 주요 UI 컴포넌트 매핑
- [x] 로딩/오류/빈 상태 정의
- [x] 주요 사용자 인터랙션 흐름 정의
- [x] KDSQ-P/KDSQ-C 판정 기준 통일
- [x] `totalScore` 계산/저장 정책 통일
- [x] 별도 솔루션/통계 화면 중복 제거
- [x] 관리자 삭제를 soft delete로 통일
- [x] 디자인 토큰 정리
- [ ] 실제 구현 후 화면 캡처 첨부
