# React 컴포넌트 설계서 — 인지기능검사 서비스

## 문서 정보
| 항목 | 내용 |
|------|------|
| **프로젝트명** | KDSQ 인지선별검사 플랫폼 (KDSQ Cognitive Screening Platform) |
| **작성자** | 서다영 황지영 윤수연 |
| **작성일** | 2026-09-26 |
| **버전** | v2.5 |
| **검토자** | 강찬식 서다영 황지영 윤수연 |

### 변경 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| v1.6 | 2026-09-24 | API를 surveys/results 기준으로 변경(대안 B), 검사 1회당 결과 1건 저장(1차 답변 보관 후 2차와 함께 제출), 채점·판정은 서버, 타입 `Exam` → `SurveyResult`, `examStore` → `resultStore`, 에러 `error.code` 분기 |
| v1.7 | 2026-09-24 | 마이페이지에 본인 회원 탈퇴 추가 (`WithdrawButton`, `WithdrawDialog`, `memberStore.withdraw()`, `DELETE /api/members/me`), 탈퇴 회원 로그인 403 처리 |
| v1.8 | 2026-09-25 | 답변 선택지 점수값(0/1/2) 명시, 토큰 오류 코드 분리 반영(`ACCESS_TOKEN_EXPIRED`/`INVALID_REFRESH_TOKEN`/`UNAUTHORIZED`), 마이페이지 이력 `size=100` 조회 및 화면 페이지네이션 |
| v1.9 | 2026-09-25 | QuestionCard 내부 표시 요소(UI 설계서의 QuestionNumber·QuestionContent) 대응 관계 명시 |
| v2.0 | 2026-09-25 | 평가 기준 반영: PropTypes 기반 Props 검증(3.2) 추가, 서버 검증 오류(`error.fields`) 처리 방식 추가 |
| v2.1 | 2026-09-25 | 답변 선택지 문구를 KDSQ 원문 표기로 통일 (아니다 / 가끔(조금) 그렇다 / 자주(많이) 그렇다) |
| v2.2 | 2026-09-25 | 성능 최적화에 코드 스플리팅(`React.lazy` + `Suspense`, 페이지 단위 지연 로딩)과 라우터 예시 추가 |
| v2.3 | 2026-09-26 | 개발 착수 준비: 개발 서버 연결(Vite 프록시·경로 별칭·axios 기본 주소)과 날짜 표시 규칙(5.4) 추가 |
| v2.4 | 2026-09-26 | 차트 라이브러리 Recharts 확정(`ExamTrendChart` 예시), 회원가입 비밀번호 검증 규칙(8~20자, 영문·숫자·특수문자 각 1개 이상) 추가 |
| v2.5 | 2026-09-26 | Request/Response 예시를 REST 설계서와 일치: 비밀번호 예시를 규칙에 맞게 변경(`password123!`), 설문 목록 응답에 description 추가 |

> 범위: 사용자 화면 SCR-01 – SCR-06 (관리자 화면은 Thymeleaf로 구현하므로 제외)

---

## 1. 컴포넌트 개요

### 1.1 컴포넌트 목록

| 화면 ID | 화면명 | 페이지 컴포넌트 | URL | 권한 |
|---|---|---|---|---|
| SCR-01 | 로그인 | `LoginPage` | `/login` | 공개 |
| SCR-02 | 회원가입 | `RegisterPage` | `/register` | 공개 |
| SCR-03 | KDSQ-P 1차 검사 | `SurveyPage` (`type="P"`) | `/surveys/p` | 로그인 필요 |
| SCR-04 | KDSQ-C 2차 검사 | `SurveyPage` (`type="C"`) | `/surveys/c` | 로그인 필요 |
| SCR-05 | 검사 결과 | `ResultPage` | `/results/:resultId` | 로그인 필요 |
| SCR-06 | 마이페이지 | `MyPage` | `/mypage` | 로그인 필요 |

> SCR-03과 SCR-04는 화면 구성이 동일하여 `SurveyPage` 하나로 만들고 `type`으로 구분

### 1.2 목적 및 역할

- **주요 목적**: 회원가입·로그인 후 KDSQ-P(1차) → KDSQ-C(2차) 인지선별검사를 진행하고, 결과와 검사 이력을 조회하는 사용자 화면 구현
- **위치**: `src/pages/`(페이지), `src/components/`(레이아웃·공통)
- **컴포넌트 유형**: 페이지 / 레이아웃 / 기능 / 공통 컴포넌트

### 1.3 기능 요구사항

- [ ] 로그인·회원가입 (입력 검증, 401 실패 문구, 409 이메일 중복 안내)
- [ ] KDSQ-P 5문항 검사 → 1차 합계 0–3점이면 결과 제출 후 결과 화면 / 4–10점이면 제출 없이 1차 답변을 보관하고 KDSQ-C 안내 후 `/surveys/c`
- [ ] KDSQ-C 15문항 검사 → 1·2차 답변을 함께 제출 → 서버 채점 결과 화면
- [ ] `/surveys/c`에 1차 답변 없이 들어오면 `/surveys/p`로 이동
- [ ] 검사 결과 표시 (총점·판정, KDSQ-C는 영역별 점수 + 관리 안내(`solutions`))
- [ ] 마이페이지 (회원정보 조회 전용, 검사 이력, 점수 추이, 이력 없음 안내, 회원 탈퇴)
- [ ] 로딩 / 오류 / 빈 데이터 상태 처리, 비로그인 접근 시 `/login` 이동

> 점수 채점과 위험도 판정은 서버(results)가 담당한다. 프론트는 1차 합계로 2차 진행 여부만 판단하고, 결과는 서버 응답 값을 그대로 표시한다.

---

## 2. 컴포넌트 구조 설계

### 2.1 컴포넌트 트리

```
UserLayout                              ← 공통 레이아웃 (Header + Outlet + Footer)
  ├── LoginPage                         ← 상태 소유, 로그인 API 호출
  │     └── LoginForm                   ← (Props: onSubmit, isLoading, errorMessage) 입력·검증
  ├── RegisterPage                      ← 상태 소유, 회원가입 API 호출, 입력 폼
  └── ProtectedRoute                    ← 비로그인 시 /login 이동
        ├── SurveyPage                  ← 상태 소유, 문항 조회·결과 제출 (P/C 공용)
        │     ├── SurveyHeader          ← (Props: type) 검사명·안내 문구
        │     ├── ProgressBar           ← (Props: current, total) 진행률
        │     ├── QuestionCard          ← (Props: question) 문항 표시 — 내부에 문항 번호·내용 표시 (UI 설계서의 QuestionNumber·QuestionContent 요소)
        │     │     └── AnswerOptions   ← (Props: value, onChange) 답변 선택
        │     ├── PreviousButton        ← (Props: isDisabled, onClick) 이전 문항
        │     ├── NextButton            ← (Props: onClick) 다음 문항
        │     └── SubmitButton          ← (Props: isSubmitting, onClick) 마지막 문항 제출
        ├── ResultPage                  ← 상태 소유, 결과 조회 API 호출
        │     ├── ResultHeader          ← 결과 제목
        │     ├── ResultStatus          ← (Props: result) 정상/주의/위험 표시
        │     ├── TotalScore            ← (Props: score, maxScore) 총점
        │     ├── ScoreSummary          ← (Props: memoryScore, otherScore, adlScore) 영역별 점수 (KDSQ-C)
        │     ├── ExamDate              ← (Props: date) 검사일시
        │     ├── Recommendation        ← (Props: solutions) 관리·추가 검진 안내 (KDSQ-C)
        │     └── HistoryButton         ← (Props: onClick) /mypage 이동
        └── MyPage                      ← 상태 소유, 회원정보·이력 조회 API 호출
              ├── ProfileCard           ← (Props: member) 회원정보 조회
              │     └── ReadOnlyNotice  ← 수정 불가 안내
              ├── ExamHistoryTable      ← (Props: results, onDetail) 검사 이력 목록
              │     └── RiskBadge       ← (Props: riskLevel) 판정 배지
              ├── ExamTrendChart        ← (Props: results) 점수 추이
              ├── EmptyHistory          ← (Props: onStart) 이력 없음 안내
              ├── WithdrawButton        ← (Props: onClick) 회원 탈퇴 버튼
              └── WithdrawDialog        ← (Props: open, isSubmitting, errorMessage, onConfirm, onCancel) 탈퇴 확인
```

> 로딩·오류 표시는 공통 `LoadingSpinner` / `Alert`를 사용합니다. 컴포넌트 이름(`Exam*`)은 화면 용어('검사')를 따르며, 데이터 타입은 `SurveyResult`를 사용합니다.

### 2.2 컴포넌트 분류 및 책임

| 컴포넌트 | 타입 | 파일 위치 | 주요 책임 | 상태 관리 | 재사용성 |
|---|---|---|---|---|---|
| `LoginPage` | 페이지 | `pages/Login/index.jsx` | 로그인 처리, `/surveys/p` 이동 | Zustand + 로컬 | 낮음 |
| `RegisterPage` | 페이지 | `pages/Register/index.jsx` | 회원가입 입력·검증·처리 | 로컬 | 낮음 |
| `SurveyPage` | 페이지 | `pages/Survey/index.jsx` | 문항 진행, 1차 분기, 1차 답변 보관, 결과 제출 | Zustand + 로컬 | 보통 (P/C 공용) |
| `ResultPage` | 페이지 | `pages/Result/index.jsx` | 결과 조회·표시 | Zustand | 낮음 |
| `MyPage` | 페이지 | `pages/MyPage/index.jsx` | 회원정보·이력 조회 | Zustand | 낮음 |
| `LoginForm` | 기능 | `pages/Login/components/` | 로그인 입력·검증 | 로컬 | 보통 |
| `SurveyHeader` 외 7종 | 기능 | `pages/Survey/components/` | 검사 화면 표시 | 없음 | 보통 |
| `ResultHeader` 외 6종 | 기능 | `pages/Result/components/` | 결과 화면 표시 | 없음 | 보통 |
| `ProfileCard` 외 6종 | 기능 | `pages/MyPage/components/` | 마이페이지 표시, 회원 탈퇴 버튼·확인 대화상자 | 없음 | 보통 |
| `UserLayout`, `Header`, `Footer` | 레이아웃 | `components/layout/` | 공통 레이아웃, 네비게이션, 로그아웃 | `Header`만 Zustand | 높음 |
| `ProtectedRoute`, `LoadingSpinner`, `Alert` | 공통 | `components/common/` | 접근 제어, 로딩·오류/안내 표시 | `ProtectedRoute`만 Zustand | 높음 |

### 2.3 파일 구조

```
src/
├── pages/
│   └── [Login | Register | Survey | Result | MyPage]/
│       ├── index.jsx          # 페이지 컴포넌트 (상태 소유)
│       └── components/        # 이 페이지에서만 쓰는 컴포넌트
├── components/
│   ├── layout/                # UserLayout, Header, Footer
│   ├── common/                # ProtectedRoute, LoadingSpinner
│   └── ui/                    # shadcn/ui (Button, Input, Alert, Card, Badge, Table, Progress, RadioGroup, Chart)
├── store/                     # authStore, surveyStore, resultStore, memberStore
├── api/                       # axiosInstance, authApi, surveyApi, resultApi, memberApi
├── types/propTypes.js         # 도메인 타입의 PropTypes shape (3.2)
└── utils/
    ├── kdsq.js                # 1차 합계(2차 진행 판단용), 판정 라벨·만점 등 표시용 상수
    ├── date.js                # 날짜 표시 형식 변환 (5.4)
    └── validation.js          # 회원가입 입력 검증 규칙 (4.2)
```

---

## 3. Props 설계

### 3.1 Props 인터페이스

| 컴포넌트 | Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|---|
| `LoginForm` | `onSubmit` | `(email, password) => void` | 필수 | — | 검증 통과 후 호출 |
| | `isLoading` | `boolean` | 선택 | `false` | 로그인 중 버튼 비활성화 |
| | `errorMessage` | `string` | 선택 | `''` | 로그인 실패 문구 |
| `SurveyPage` | `type` | `'P' \| 'C'` | 필수 | — | 검사 종류 |
| `SurveyHeader` | `type` | `'P' \| 'C'` | 필수 | — | 검사명·안내 문구 결정 |
| `ProgressBar` | `current`, `total` | `number` | 필수 | — | 진행률 (예: 7 / 15) |
| `QuestionCard` | `question` | `Question` | 필수 | — | 현재 문항 |
| `AnswerOptions` | `value` | `0 \| 1 \| 2 \| null` | 필수 | — | 선택한 답변 점수 (아니다=0, 가끔(조금) 그렇다=1, 자주(많이) 그렇다=2) |
| | `onChange` | `(value) => void` | 필수 | — | 답변 선택 시 호출 |
| `PreviousButton` | `isDisabled` | `boolean` | 선택 | `false` | 첫 문항에서 비활성화 |
| | `onClick` | `() => void` | 필수 | — | 이전 문항 이동 |
| `NextButton` | `onClick` | `() => void` | 필수 | — | 다음 문항 이동 |
| `SubmitButton` | `isSubmitting` | `boolean` | 선택 | `false` | 제출 중 비활성화 |
| | `onClick` | `() => void` | 필수 | — | 마지막 문항 제출 |
| `ResultStatus` | `result` | `SurveyResult` | 필수 | — | 판정 문구 표시 |
| `TotalScore` | `score` | `number` | 필수 | — | 총점 (KDSQ-P: `firstScore`, KDSQ-C: `totalScore`) |
| | `maxScore` | `number` | 선택 | — | 만점 (KDSQ-P: 10, KDSQ-C: 30) |
| `ScoreSummary` | `memoryScore`, `otherScore`, `adlScore` | `number` | 필수 | — | 영역별 점수 (KDSQ-C 결과에서만 렌더링) |
| `ExamDate` | `date` | `string` | 필수 | — | 검사일시 |
| `Recommendation` | `solutions` | `Solution[]` | 필수 | — | 결과 응답의 관리 안내 목록 (정상이면 빈 배열 → 미렌더) |
| `HistoryButton` | `onClick` | `() => void` | 필수 | — | `/mypage` 이동 |
| `ProfileCard` | `member` | `Member` | 필수 | — | 회원정보 |
| `ExamHistoryTable` | `results` | `SurveyResult[]` | 필수 | — | 검사 이력 (전체 이력을 받아 10건씩 페이지를 나눠 표시) |
| | `onDetail` | `(resultId) => void` | 필수 | — | [보기] 클릭 시 호출 |
| `RiskBadge` | `riskLevel` | `'Normal' \| 'Borderline' \| 'HighRisk'` | 필수 | — | 정상/주의/위험 배지 |
| `ExamTrendChart` | `results` | `SurveyResult[]` | 필수 | — | 점수 추이 데이터 (Recharts `LineChart`로 표시) |
| `EmptyHistory` | `onStart` | `() => void` | 필수 | — | 검사 시작 (`/surveys/p`) |
| `WithdrawButton` | `onClick` | `() => void` | 필수 | — | 탈퇴 확인 대화상자 열기 |
| `WithdrawDialog` | `open` | `boolean` | 필수 | — | 대화상자 표시 여부 |
| | `isSubmitting` | `boolean` | 선택 | `false` | 탈퇴 요청 중 버튼 비활성화 |
| | `errorMessage` | `string` | 선택 | `''` | 탈퇴 실패 문구 |
| | `onConfirm` | `() => void` | 필수 | — | [탈퇴] 클릭 시 호출 |
| | `onCancel` | `() => void` | 필수 | — | [취소] 클릭 시 호출 |

> **도메인 타입 정의 (공통 참조)**
> ```typescript
> type Member = {
>   name: string;                    // 이름
>   email: string;                   // 이메일
>   gender: 'MALE' | 'FEMALE';       // 성별 (남성/여성)
>   birthYear: number;               // 출생년도
> }
>
> type Survey = {
>   id: number;                      // 설문 ID
>   examType: 'KDSQ_P' | 'KDSQ_C';   // 검사 종류
>   title: string;                   // 설문 제목
>   description: string;             // 검사 안내 문구 (SurveyHeader에 표시)
> }
>
> type Question = {
>   id: number;                      // 문항 ID
>   questionNumber: number;          // 문항 번호
>   content: string;                 // 문항 내용
> }
>
> type Answer = {
>   questionId: number;              // 문항 ID
>   score: number;                   // 선택한 답변 점수
> }
>
> type Solution = {
>   id: number;
>   title: string;                   // 안내 제목
>   content: string;                 // 권고사항 내용
> }
>
> type SurveyResult = {
>   id: number;                      // 검사 결과 ID
>   examType: 'KDSQ_P' | 'KDSQ_C';   // 마지막으로 완료한 검사 종류
>   firstScore: number;              // KDSQ-P 점수 (10점 만점, 모든 결과에 존재)
>   memoryScore: number | null;      // KDSQ-C 기억력 (10점 만점), KDSQ-P로 끝나면 null
>   otherScore: number | null;       // KDSQ-C 기타 인지기능 (10점 만점)
>   adlScore: number | null;         // KDSQ-C 일상생활수행능력 (10점 만점)
>   totalScore: number | null;       // 서버 계산 (30점 만점), KDSQ-P로 끝나면 null
>   riskLevel: 'Normal' | 'Borderline' | 'HighRisk';  // 정상/주의/위험
>   createdAt: string;               // 검사일시 (한국 시간 "yyyy-MM-ddTHH:mm:ss")
>   solutions?: Solution[];          // 결과 상세 조회 시에만 포함
> }
> // null 점수는 화면에 "-"로 표시
> ```

### 3.2 Props 검증 (PropTypes)

프로젝트는 JavaScript(JSX)로 작성하므로 TypeScript 대신 `prop-types` 라이브러리로 Props를 검증한다. 3.1 표의 타입·필수여부·기본값을 그대로 코드로 옮기며, 개발 모드에서 잘못된 Props가 전달되면 브라우저 콘솔에 경고가 출력된다.

**작성 규칙**

| 규칙 | 적용 방법 |
|---|---|
| 모든 컴포넌트에 선언 | Props를 받는 컴포넌트는 `Component.propTypes`를 반드시 작성한다 |
| 필수 Props | 3.1 표의 "필수"는 `.isRequired`를 붙인다 |
| 기본값 | 3.1 표의 기본값은 함수 매개변수 기본값으로 지정한다 (`{ isLoading = false }`) |
| 고정 값 목록 | `'P' \| 'C'`, 판정 등급처럼 값이 정해진 Props는 `PropTypes.oneOf([...])` |
| 도메인 객체 | `Question`, `SurveyResult` 등은 `src/types/propTypes.js`에 `PropTypes.shape`로 한 번만 정의하고 가져다 쓴다 |
| 콜백 | `onSubmit`, `onChange` 등 함수 Props는 `PropTypes.func` |

**공통 shape 정의 (`src/types/propTypes.js`)**

```jsx
import PropTypes from 'prop-types';

export const RiskLevelType = PropTypes.oneOf(['Normal', 'Borderline', 'HighRisk']);

export const QuestionShape = PropTypes.shape({
  id: PropTypes.number.isRequired,
  questionNumber: PropTypes.number.isRequired,
  content: PropTypes.string.isRequired,
});

export const SolutionShape = PropTypes.shape({
  id: PropTypes.number.isRequired,
  title: PropTypes.string.isRequired,
  content: PropTypes.string.isRequired,
});

export const SurveyResultShape = PropTypes.shape({
  id: PropTypes.number.isRequired,
  examType: PropTypes.oneOf(['KDSQ_P', 'KDSQ_C']).isRequired,
  firstScore: PropTypes.number.isRequired,
  memoryScore: PropTypes.number,      // KDSQ-P로 끝나면 null
  otherScore: PropTypes.number,
  adlScore: PropTypes.number,
  totalScore: PropTypes.number,
  riskLevel: RiskLevelType.isRequired,
  createdAt: PropTypes.string.isRequired,
  solutions: PropTypes.arrayOf(SolutionShape),
});

export const MemberShape = PropTypes.shape({
  name: PropTypes.string.isRequired,
  email: PropTypes.string.isRequired,
  gender: PropTypes.oneOf(['MALE', 'FEMALE']).isRequired,
  birthYear: PropTypes.number.isRequired,
});
```

**컴포넌트 적용 예시**

```jsx
// src/pages/Survey/components/AnswerOptions.jsx
import React from 'react';
import PropTypes from 'prop-types';
import { RadioGroup } from '@/components/ui/radio-group';

const OPTIONS = [
  { label: '아니다', value: 0 },
  { label: '가끔(조금) 그렇다', value: 1 },
  { label: '자주(많이) 그렇다', value: 2 },
];

function AnswerOptions({ value, onChange }) {
  return (
    <RadioGroup value={value === null ? '' : String(value)} onValueChange={(v) => onChange(Number(v))}>
      {OPTIONS.map((o) => (/* 선택지 렌더링 */))}
    </RadioGroup>
  );
}

AnswerOptions.propTypes = {
  value: PropTypes.oneOf([0, 1, 2]),          // 선택 전에는 null
  onChange: PropTypes.func.isRequired,
};

export default React.memo(AnswerOptions);
```

```jsx
// src/pages/Result/components/Recommendation.jsx
import PropTypes from 'prop-types';
import { SolutionShape } from '@/types/propTypes';

function Recommendation({ solutions }) {
  if (solutions.length === 0) return null;   // 정상 결과는 렌더링하지 않음
  return (/* 관리 안내 목록 */);
}

Recommendation.propTypes = {
  solutions: PropTypes.arrayOf(SolutionShape).isRequired,
};
```

> `value`처럼 "필수지만 null일 수 있는" Props는 `.isRequired`를 붙이면 null일 때 경고가 나므로, `.isRequired` 없이 선언하고 3.1 표의 필수여부로 의도를 표시한다.

---

## 4. 상태 관리 설계

### 4.1 상태 위치 결정 근거

| 상태명 | 위치 선택 | 결정 근거 |
|---|---|---|
| `currentIndex`, `answers` | 로컬 `useState` (`SurveyPage`) | 검사 화면 안에서만 사용, 문항별 응답은 저장하지 않음 |
| 폼 입력값, `isLoading`, `errorMessage` | 로컬 `useState` (`LoginPage`, `RegisterPage`) | 해당 화면의 입력·제출 상태 |
| `surveys`, `questions` | `surveyStore` | 서버 데이터 (설문 목록·문항 조회) |
| `pendingFirstAnswers` | `resultStore` (sessionStorage 유지) | 1차(`/surveys/p`) → 2차(`/surveys/c`) 화면 사이에 유지되어야 하고 새로고침에도 남아야 함. 2차 제출 성공·로그아웃 시 비움 |
| `currentResult`, `history` | `resultStore` | 서버 데이터, 검사·결과·마이페이지에서 사용 |
| `me` | `memberStore` | 서버 데이터 (회원정보 조회) |
| `accessToken`, `refreshToken`, `email` | `authStore` (localStorage) | 앱 전체 인증 상태, 새로고침 후 유지 |

> `totalScore`, 1차 합계처럼 계산 가능한 값은 상태에 저장하지 않습니다.

### 4.2 로컬 상태 (useState)

| 상태명   | 타입     | 초기값   | 용도    |
| ------- | -------- |----------- | ------ |
| `LoginPage.email`   | `string`   | `""`   | 로그인 이메일 입력값  |
| `LoginPage.password` | `string`   | `""`   | 로그인 비밀번호 입력값   |
| `LoginPage.isLoading`  | `boolean`  | `false`   | 로그인 API 요청 진행 여부  |
| `LoginPage.errorMessage`  | `string`    | `""`  | 로그인 실패 및 입력 오류 메시지  |
| `RegisterPage.form`  | `object` | `{ name: "", email: "", password: "", passwordConfirm: "", gender: "", birthYear: "" }` | 회원가입 입력값   |
| `RegisterPage.errors`  | `object`  | `{}`   | 회원가입 필드별 유효성 검증 오류 (화면 검증 결과 + 서버 `error.fields`, `{ email: "메시지" }` 형태)  |
| `RegisterPage.isLoading` | `boolean`   | `false` | 회원가입 API 요청 진행 여부  |
| `RegisterPage.errorMessage` | `string`  | `""`   | 회원가입 실패 메시지   |
| `SurveyPage.currentIndex`   | `number`   | `0`  | 현재 검사 문항의 인덱스  |
| `SurveyPage.answers` | `Record<number, number>` | `{}`  | 문항별 사용자의 선택 답변 (questionId → score)  |
| `SurveyPage.submitting` | `boolean` | `false`  | 검사 결과 제출 진행 여부  |
| `ResultPage.isLoading` | `boolean`   | `true` | 검사 결과 조회 진행 여부  |
| `ResultPage.errorMessage` | `string`  | `""`  | 검사 결과 조회 오류 메시지  |
| `MyPage.isLoading`  | `boolean`  | `true` | 회원정보·검사 이력 조회 진행 여부 |
| `MyPage.isWithdrawOpen` | `boolean` | `false` | 탈퇴 확인 대화상자 표시 여부 |
| `MyPage.isWithdrawing` | `boolean` | `false` | 탈퇴 API 요청 진행 여부 |
| `MyPage.withdrawError` | `string` | `""` | 탈퇴 실패 메시지 |
| `MyPage.historyPage` | `number` | `0` | 이력 표의 현재 페이지 (화면 페이지네이션) |
| `MyPage.errorMessage` | `string` | `""`  | 회원정보·검사 이력 조회 오류 메시지  |


**회원가입 입력 검증 규칙** (`utils/validation.js`, 서버 검증과 같은 규칙)

| 필드 | 규칙 | 오류 문구 |
|---|---|---|
| name | 공백 아님, 1~50자 | "이름을 입력해주세요." |
| email | 이메일 형식, 최대 100자 | "올바른 이메일 형식이 아닙니다." |
| password | 8~20자, 영문·숫자·특수문자 각 1개 이상, 공백 불가 | "비밀번호는 8~20자의 영문, 숫자, 특수문자를 모두 포함해야 합니다." |
| passwordConfirm | password와 일치 | "비밀번호가 일치하지 않습니다." |
| gender | 선택됨 | "성별을 선택해주세요." |
| birthYear | 1900 ~ 현재연도 | "출생년도를 올바르게 입력해주세요." |

```js
// src/utils/validation.js
export const PASSWORD_REGEX = /^(?=.*[A-Za-z])(?=.*\d)(?=.*[!-/:-@\[-`{-~])[!-~]{8,20}$/;
```

### 4.3 Zustand 스토어

| 스토어 | 상태 | 액션 |
|---|---|---|
| `authStore` | `accessToken`, `refreshToken`, `email` | `login()`, `logout()`, `setAccessToken()` |
| `surveyStore` | `surveys`(examType ↔ id 매핑), `questions`, `loading`, `error` | `fetchSurveys()`, `fetchQuestions(surveyId)` |
| `resultStore` | `currentResult`, `history`, `pendingFirstAnswers`, `loading`, `error` | `submitResult({ surveyId, firstAnswers?, answers })`, `fetchResult(resultId)`, `fetchMyResults()` (`size=100`으로 1회 조회), `setPendingFirstAnswers()`, `clearPendingFirstAnswers()` |
| `memberStore` | `me`, `loading`, `error` | `fetchMe()`, `withdraw()` (성공 시 `authStore.logout()` 호출) |

> `resultStore`의 `pendingFirstAnswers`만 Zustand `persist` 미들웨어로 sessionStorage에 보관한다 (`partialize`로 이 필드만 지정).

### 4.4 점수·판정 규칙 (`utils/kdsq.js`)

```
최종 채점·판정은 서버(results) 책임. 프론트는 응답 값을 표시만 한다.

프론트에서 쓰는 계산은 1차 합계 하나뿐 (2차 진행 여부 판단용):
  firstSum = Q1–Q5 합계 (10점 만점)
    0–3  → POST /api/results {surveyId: P, answers} → 결과 화면
    4–10 → 제출하지 않고 pendingFirstAnswers에 보관 → /surveys/c

표시용 상수: 판정 라벨(Normal=정상, Borderline=주의, HighRisk=위험), 만점(KDSQ-P 10, KDSQ-C 30)
```

---

## 5. API 연동 설계

### 5.1 API 목록

| API | 메서드 | 용도 | 호출 시점 | 인증 |
|---|---|---|---|---|
| `/api/auth/login` | POST | 로그인 | 로그인 버튼 | 불필요 |
| `/api/members` | POST | 회원가입 | 회원가입 버튼 | 불필요 |
| `/api/auth/reissue` | POST | 액세스 토큰 재발급 | 401 응답 시 (axios 인터셉터) | 불필요 (리프레시 토큰) |
| `/api/auth/logout` | POST | 로그아웃 | Header 로그아웃 | 필요 |
| `/api/surveys` | GET | 설문 목록 (surveyId 확보) | 로그인 후 1회 | 필요 |
| `/api/surveys/{surveyId}/questions` | GET | 문항 조회 | 검사 화면 진입 | 필요 |
| `/api/results` | POST | 검사 결과 제출 (서버 채점·저장) | 1차 0–3점 제출 / 2차 마지막 문항 제출 | 필요 |
| `/api/results/{resultId}` | GET | 결과 조회 (`solutions` 포함) | 결과 화면 진입 | 필요 |
| `/api/members/me` | GET | 회원정보 조회 | 마이페이지 진입 | 필요 |
| `/api/members/me/results?size=100` | GET | 검사 이력 조회 (추이 그래프·이력 표 공용) | 마이페이지 진입 | 필요 |
| `/api/members/me` | DELETE | 회원 탈퇴 (소프트 삭제) | 탈퇴 확인 대화상자의 [탈퇴] 클릭 | 필요 |

### 5.2 Request / Response 예시

#### 5.2.1 로그인

```JavaScript
// POST /api/auth/login

// Request
{
  "email": "hong@example.com",
  "password": "password123!"
}

// Response 200
{
  "success": true,
  "data": {
    "accessToken": "JWT_ACCESS_TOKEN",
    "refreshToken": "JWT_REFRESH_TOKEN",
    "tokenType": "Bearer",
    "expiresIn": 3600
  }
}
```

#### 5.2.2 회원가입

```JavaScript
// POST /api/members
// Request
{
  "name": "홍길동",
  "email": "hong@example.com",
  "password": "password123!",
  "gender": "MALE",
  "birthYear": 1960
}
// Response 201
{
  "success": true,
  "data": {
    "id": 1,
    "email": "hong@example.com",
    "name": "홍길동",
    "gender": "MALE",
    "birthYear": 1960,
    "status": "ACTIVE"
  },
  "message": "회원가입이 완료되었습니다"
}
```

#### 5.2.3 설문 목록 조회

```JavaScript
// GET /api/surveys
// Response 200
{
  "success": true,
  "data": [
    {
      "id": 1,
      "examType": "KDSQ_P",
      "title": "KDSQ-P 1차 선별검사",
      "description": "아래의 각 항목에 대하여, 1년 전과 비교하여, 현재 상태에 해당하는 곳에 표시해 주십시오. (동행한 가족이 있으면 가족이 작성하시고, 없으면 본인이 작성하십시오.)"
    },
    {
      "id": 2,
      "examType": "KDSQ_C",
      "title": "KDSQ-C 2차 상세검사",
      "description": "아래의 각 항목에 대하여, 1년 전과 비교하여, 현재 상태에 해당하는 곳에 표시해 주십시오. (동행한 가족이 있으면 가족이 작성하시고, 없으면 본인이 작성하십시오.)"
    }
  ]
}
```

#### 5.2.4 KDSQ-P 문항 조회

```JavaScript
// GET /api/surveys/1/questions
// Response 200
{
  "success": true,
  "data": [
    {
      "id": 1,
      "questionNumber": 1,
      "content": "자신의 기억력이 친구나 동료들에 비해 못하다고 생각하십니까?"
    },
    {
      "id": 2,
      "questionNumber": 2,
      "content": "..."
    }
  ]
}
```

#### 5.2.5 KDSQ-P 검사 결과 제출 (1차 합계 0–3점일 때만)

```JavaScript
// POST /api/results
// Request
{
  "surveyId": 1,
  "answers": [
    { "questionId": 1, "score": 1 },
    { "questionId": 2, "score": 0 },
    { "questionId": 3, "score": 1 },
    { "questionId": 4, "score": 0 },
    { "questionId": 5, "score": 1 }
  ]
}
// Response 201 (Location: /api/results/101)
{
  "success": true,
  "data": {
    "id": 101,
    "examType": "KDSQ_P",
    "firstScore": 3,
    "memoryScore": null,
    "otherScore": null,
    "adlScore": null,
    "totalScore": null,
    "riskLevel": "Normal",
    "createdAt": "2026-09-22T14:30:00"
  }
}
```

> 1차 합계가 4–10점이면 이 요청을 보내지 않고 `setPendingFirstAnswers(answers)` 후 `/surveys/c`로 이동한다.

#### 5.2.6 KDSQ-C 문항 조회

```JavaScript
// GET /api/surveys/2/questions
// Response 200
{
  "success": true,
  "data": [
    {
      "id": 6,
      "questionNumber": 1,
      "content": "오늘이 몇 월이고, 무슨 요일인지를 모른다."
    },
    {
      "id": 7,
      "questionNumber": 2,
      "content": "..."
    }
  ]
}
```

#### 5.2.7 KDSQ-C 검사 결과 제출 (1·2차 답변 함께)

```JavaScript
// POST /api/results
// Request
{
  "surveyId": 2,
  "firstAnswers": [            // resultStore.pendingFirstAnswers
    { "questionId": 1, "score": 1 },
    { "questionId": 2, "score": 1 }
    // ... 5개
  ],
  "answers": [
    { "questionId": 6, "score": 1 },
    { "questionId": 7, "score": 0 }
    // ... 15개
  ]
}
// Response 201 (Location: /api/results/102)
{
  "success": true,
  "data": {
    "id": 102,
    "examType": "KDSQ_C",
    "firstScore": 5,
    "memoryScore": 2,
    "otherScore": 2,
    "adlScore": 1,
    "totalScore": 5,
    "riskLevel": "Borderline",
    "createdAt": "2026-09-22T14:30:00"
  }
}
```

제출 성공 시 `clearPendingFirstAnswers()` 후 `/results/102`로 이동한다. `totalScore`와 `riskLevel`은 서버가 계산한 값을 그대로 사용하며, 문항별 `answers`는 백엔드에 저장하지 않는다.

#### 5.2.8 검사 결과 조회

```JavaScript
// GET /api/results/102
// Response 200
{
  "success": true,
  "data": {
    "id": 102,
    "examType": "KDSQ_C",
    "firstScore": 5,
    "memoryScore": 2,
    "otherScore": 2,
    "adlScore": 1,
    "totalScore": 5,
    "riskLevel": "Borderline",
    "createdAt": "2026-09-22T14:30:00",
    "solutions": [
      { "id": 1, "title": "인지건강 관리 안내", "content": "..." }
    ]
  }
}
```

#### 5.2.9 회원 정보 조회

```JavaScript
// GET /api/members/me
// Response 200
{
  "success": true,
  "data": {
    "name": "홍길동",
    "email": "hong@example.com",
    "gender": "MALE",
    "birthYear": 1960
  }
}
```

#### 5.2.10 검사 이력 조회

```JavaScript
// GET /api/members/me/results?size=100
// 추이 그래프는 content 전체로 그리고, 이력 표는 화면에서 10건씩 나눠 표시
// Response 200
{
  "success": true,
  "data": {
    "content": [
      {
        "id": 102,
        "examType": "KDSQ_C",
        "firstScore": 5,
        "memoryScore": 2,
        "otherScore": 2,
        "adlScore": 1,
        "totalScore": 5,
        "riskLevel": "Borderline",
        "createdAt": "2026-09-22T14:30:00"
      },
      {
        "id": 101,
        "examType": "KDSQ_P",
        "firstScore": 3,
        "memoryScore": null,
        "otherScore": null,
        "adlScore": null,
        "totalScore": null,
        "riskLevel": "Normal",
        "createdAt": "2026-06-20T10:00:00"
      }
    ],
    "page": { "number": 0, "size": 20, "totalElements": 2, "totalPages": 1, "first": true, "last": true }
  }
}
```

#### 5.2.11 회원 탈퇴

```JavaScript
// DELETE /api/members/me
// Request: 본문 없음 (Authorization 헤더의 토큰으로 본인 확인)
// Response 204 No Content (본문 없음)

// 처리 순서
await memberApi.withdraw();          // DELETE /api/members/me
authStore.logout();                  // 토큰·스토어 초기화
navigate('/login', { replace: true, state: { message: '회원 탈퇴가 완료되었습니다.' } });
```

### 5.3 에러 처리

에러 응답은 `{ success: false, error: { code, message } }` 형식이며, HTTP 상태가 아니라 `error.code`로 분기한다.

| error.code | 처리 |
|---|---|
| `VALIDATION_ERROR` | `error.fields`(`[{ field, message }]`)를 `{ field: message }` 객체로 바꿔 `RegisterPage.errors`에 넣고 각 입력칸 아래에 표시 |
| `INVALID_CREDENTIALS` | 로그인: "이메일 또는 비밀번호를 확인해주세요." |
| `ACCESS_TOKEN_EXPIRED` | axios 응답 인터셉터에서 `POST /api/auth/reissue` 후 원래 요청 1회 재시도 |
| `INVALID_REFRESH_TOKEN` | 재발급 실패. 다시 재발급을 시도하지 않고 `authStore.logout()` 후 `/login` ("다시 로그인해주세요.") |
| `UNAUTHORIZED` | `authStore.logout()` 후 `/login` |
| `DUPLICATE_EMAIL` | "이미 가입된 이메일입니다." |
| `MEMBER_WITHDRAWN` | 로그인: "이용할 수 없는 계정입니다." (탈퇴 회원) |
| `KDSQ_C_NOT_ALLOWED`, `KDSQ_C_REQUIRED` | "1차 검사부터 다시 진행해주세요." 안내 후 `clearPendingFirstAnswers()`, `/surveys/p` 이동 |
| `INVALID_ANSWER_COUNT` | "모든 문항에 답해주세요." |
| `RESULT_NOT_FOUND` 및 그 외 (500, 네트워크) | 오류 메시지 + 재시도 |

### 5.4 개발 서버 연결 및 공통 설정

React 개발 서버(Vite, 5173 포트)와 Spring 서버(8080 포트)는 주소가 달라 그대로 호출하면 CORS 오류가 난다. Vite 프록시로 `/api` 요청을 8080으로 넘겨, 브라우저에서는 같은 주소로 보이게 한다. (Spring에 CORS 설정 불필요)

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },   // import '@/components/...'
  },
  server: {
    port: 5173,
    proxy: {
      '/api': { target: 'http://localhost:8080', changeOrigin: true },
    },
  },
});
```

```js
// src/api/axiosInstance.js
const axiosInstance = axios.create({
  baseURL: '/api',                  // 프록시를 거쳐 http://localhost:8080/api 로 전달
  headers: { 'Content-Type': 'application/json' },
});
// 요청 인터셉터: authStore.accessToken → Authorization: Bearer {토큰}
// 응답 인터셉터: 5.3 에러 처리 (ACCESS_TOKEN_EXPIRED → 재발급 후 1회 재시도)
```

| 항목 | 규칙 |
|------|------|
| API 주소 | 코드에서는 항상 `/api/...` 상대 경로로 호출하고 `http://localhost:8080`을 직접 쓰지 않는다 |
| 경로 별칭 | `@/` = `src/` (shadcn/ui 기본 설정과 같음) |
| 날짜 표시 | 서버는 한국 시간 `"2026-09-24T10:30:00"` 형식으로 보낸다 (REST API 설계서 2.5). 화면에는 `utils/date.js`의 `formatDateTime()`으로 `2026-09-24 10:30`, 그래프 축에는 `formatDate()`로 `09.24` 형태로 표시한다. 문자열에 시간대가 없으므로 `new Date()` 변환 시 브라우저 시간(한국)으로 해석된다 |

### 5.5 차트 (Recharts)

마이페이지 점수 추이 그래프는 Recharts로 그린다. shadcn/ui의 Chart 컴포넌트가 Recharts 기반이라 디자인 토큰(색상)을 그대로 쓸 수 있다.

```jsx
// src/pages/MyPage/components/ExamTrendChart.jsx
import React, { useMemo } from 'react';
import PropTypes from 'prop-types';
import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';
import { SurveyResultShape } from '@/types/propTypes';
import { formatDate } from '@/utils/date';

function ExamTrendChart({ results }) {
  const data = useMemo(() =>
    [...results].reverse().map((r) => ({                  // 오래된 검사부터 왼쪽에 표시
      date: formatDate(r.createdAt),                      // "09.24"
      score: r.totalScore ?? r.firstScore,                // KDSQ-C는 총점, KDSQ-P는 1차 점수
      examType: r.examType,
      riskLevel: r.riskLevel,
    })), [results]);

  return (
    <ResponsiveContainer width="100%" height={280}>       {/* 반응형: 가로 100% */}
      <LineChart data={data}>
        <XAxis dataKey="date" />
        <YAxis domain={[0, 30]} />                         {/* y축 0~30 고정 (UI 설계서 3.6.7) */}
        <Tooltip />                                        {/* 검사 종류·점수·판정 표시 */}
        <Line type="monotone" dataKey="score" />
      </LineChart>
    </ResponsiveContainer>
  );
}

ExamTrendChart.propTypes = { results: PropTypes.arrayOf(SurveyResultShape).isRequired };
export default React.memo(ExamTrendChart);
```

---

## 6. 컴포넌트 구현 골격

> 모든 페이지 컴포넌트는 아래 순서를 따릅니다. 상세 구현은 필요.

```jsx
// src/pages/[PageName]/index.jsx

import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuthStore } from '@/store/authStore';
import { useSurveyStore } from '@/store/surveyStore';
import { useResultStore } from '@/store/resultStore';
import { useMemberStore } from '@/store/memberStore';
import [ChildComponent] from './components/[ChildComponent]';
import LoadingSpinner from '@/components/common/LoadingSpinner';
import Alert from '@/components/common/Alert';

export default function [ComponentName]({ /* Props */ }) {

  // ── 1. 로컬 상태 ─────────────────────────────
  const [state, setState] = useState(initialValue);

  // ── 2. Zustand 전역 상태 ─────────────────────
  const { /* state, actions */ } = use[Store]();
  const navigate = useNavigate();

  // ── 3. 사이드 이펙트 (데이터 로드) ───────────
  useEffect(() => {
    // 화면 진입 시 필요한 데이터 조회
  }, []);

  // ── 4. 이벤트 핸들러 ─────────────────────────
  const handleAction = () => {
    //
  };

  // ── 5. 조건부 렌더링 (로딩/에러/빈 데이터) ───
  if (loading) {
    return <LoadingSpinner />;
  }
  if (error) {
    return <ErrorMessage />;
  }

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div className="">
      {/* 하위 컴포넌트 배치 */}
      <[HeaderComponent] />
      <[ChildComponent]
        data={data}
        onAction={handleAction}
      />
    </div>
  );
}
```

**SurveyPage 제출 핸들러 예시**

```jsx
const handleSubmit = async () => {
  const answerList = Object.entries(answers).map(([questionId, score]) => ({ questionId: Number(questionId), score }));

  if (type === 'P') {
    const firstSum = sumScores(answerList);            // utils/kdsq.js
    if (firstSum >= 4) {
      setPendingFirstAnswers(answerList);              // 저장하지 않고 보관
      navigate('/surveys/c');
      return;
    }
    const result = await submitResult({ surveyId: surveyIdOf('KDSQ_P'), answers: answerList });
    navigate(`/results/${result.id}`);
  } else {
    const result = await submitResult({
      surveyId: surveyIdOf('KDSQ_C'),
      firstAnswers: pendingFirstAnswers,
      answers: answerList,
    });
    clearPendingFirstAnswers();
    navigate(`/results/${result.id}`);
  }
};

// type === 'C'로 진입했는데 pendingFirstAnswers가 없으면 /surveys/p로 이동
```

---

## 7. 성능 최적화

- [x] `React.memo` — `QuestionCard`, `AnswerOptions`, `ExamTrendChart`
- [x] `useCallback` — `AnswerOptions`에 전달하는 답변 선택 핸들러
- [x] `useMemo` — 마이페이지 이력 정렬, 차트 데이터 변환
- [x] 코드 스플리팅 — 페이지 컴포넌트를 `React.lazy`로 지연 로딩하여 첫 화면(로그인)에서 필요한 코드만 내려받음. 차트 라이브러리처럼 무거운 코드는 `MyPage` 진입 시에만 로드

**라우터 코드 스플리팅 예시 (`src/App.jsx`)**

```jsx
import { lazy, Suspense } from 'react';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import UserLayout from '@/components/layout/UserLayout';
import ProtectedRoute from '@/components/common/ProtectedRoute';
import LoadingSpinner from '@/components/common/LoadingSpinner';

// 페이지 단위로 번들을 나눔 (해당 URL에 처음 들어갈 때 다운로드)
const LoginPage = lazy(() => import('@/pages/Login'));
const RegisterPage = lazy(() => import('@/pages/Register'));
const SurveyPage = lazy(() => import('@/pages/Survey'));
const ResultPage = lazy(() => import('@/pages/Result'));
const MyPage = lazy(() => import('@/pages/MyPage'));

export default function App() {
  return (
    <BrowserRouter>
      <Suspense fallback={<LoadingSpinner />}>
        <Routes>
          <Route element={<UserLayout />}>
            <Route path="/login" element={<LoginPage />} />
            <Route path="/register" element={<RegisterPage />} />
            <Route element={<ProtectedRoute />}>
              <Route path="/surveys/p" element={<SurveyPage type="P" />} />
              <Route path="/surveys/c" element={<SurveyPage type="C" />} />
              <Route path="/results/:resultId" element={<ResultPage />} />
              <Route path="/mypage" element={<MyPage />} />
            </Route>
            <Route path="*" element={<Navigate to="/login" replace />} />
          </Route>
        </Routes>
      </Suspense>
    </BrowserRouter>
  );
}
```

> 페이지를 불러오는 동안 `Suspense`의 `fallback`으로 공통 `LoadingSpinner`를 보여준다. 공통 컴포넌트(`Header`, `ProtectedRoute`)와 스토어는 모든 화면에서 쓰므로 나누지 않는다.

---

## 8. 작성 체크리스트

- [x] 화면 설계서의 컴포넌트를 모두 반영했는가?
- [x] 컴포넌트 트리(부모-자식 계층)가 작성되었는가?
- [x] 각 상태가 왜 로컬/전역인지 근거가 있는가?
- [x] Props의 타입, 필수여부, 기본값이 작성되었는가?
- [x] Props를 PropTypes로 검증하는 규칙과 예시가 있는가? (v2.0)
- [x] 사용하는 API와 호출 시점이 명확한가?
- [x] 로딩 / 에러 / 빈 데이터 상태가 고려되었는가?
- [x] API 경로·필드명이 REST API 설계서와 일치하는가?
